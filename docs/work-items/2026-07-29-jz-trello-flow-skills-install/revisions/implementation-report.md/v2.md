Work item: 2026-07-29-jz-trello-flow-skills-install
Artifact: implementation
Revision: 2
Prerequisites: contract@1,plan@1,approval@1
Status: superseded

# Changed paths

- `NEXT.md` (pipeline step only)
- `packages/trello-work-cli/package.json`
- `packages/trello-work-cli/README.md`
- `packages/trello-work-cli/assets/work-guide.md`
- `packages/trello-work-cli/assets/agent-skills/agent-workflow-protocol.md`
- `packages/trello-work-cli/assets/agent-skills/trello-work-orchestrator/SKILL.md`
- `packages/trello-work-cli/assets/agent-skills/trello-work-design/SKILL.md`
- `packages/trello-work-cli/assets/agent-skills/trello-work-deliver/SKILL.md`
- `packages/trello-work-cli/assets/agent-skills/trello-work-recover/SKILL.md`
- `packages/trello-work-cli/src/skills-install.ts`
- `packages/trello-work-cli/src/skills-install.test.ts`
- `packages/trello-work-cli/src/cli.ts`
- `packages/trello-work-cli/src/cli.test.ts`
- `packages/trello-work-cli/src/command-catalog.ts`
- `packages/trello-work-cli/src/docs.ts`
- `packages/trello-work-cli/src/docs.test.ts`
- `packages/trello-work-cli/test/skills-install.e2e.test.ts`
- `docs/work-items/2026-07-29-jz-trello-flow-skills-install/revisions/verification.md/v1.md`
- `docs/work-items/2026-07-29-jz-trello-flow-skills-install/revisions/implementation-report.md/v1.md`
- `docs/work-items/2026-07-29-jz-trello-flow-skills-install/implementation-report.md`

The two revision archives preserve the failed verification and superseded implementation attempt required by failed-verification recovery. Canonical `.agents/skills/trello-work-*` source bytes remain unchanged.

## Decisions

- Repository discovery now asks `git -C <cwd> rev-parse --show-toplevel`; an arbitrary `.git` file or directory is not accepted as evidence of a repository, and the offline route still never enters Trello configuration or client code.
- Before reading replacement state or creating `.agents/skills`, the installer rejects any existing symbolic link or junction at `.agents`, `.agents/skills`, or any of the four managed targets with `SKILLS_PATH_REDIRECTED`.
- Validator provenance no longer trusts an unqualified `skills-ref` executable from `PATH`. The operator explicitly supplies `JZ_TRELLO_FLOW_SKILLS_REF_CHECKOUT` and `JZ_TRELLO_FLOW_SKILLS_REF_PYTHON`; the installer proves that the checkout is its actual Git top level, is exactly `agentskills/agentskills@38a2ff82958afee88dadf4831509e6f7e9d8ef4e`, and has no modified or untracked `skills-ref` source, then imports `skills_ref` directly from that pinned checkout through the configured Python environment.
- Rollback never recursively deletes a replacement before securing recoverability. It moves failed new content into the transaction, restores backups by rename, and removes the transaction only after full success or full restoration. If any restoration fails, it returns `SKILLS_RECOVERY_REQUIRED`, preserves the complete transaction/backups, and reports the exact path after `Recovery data preserved at:`.
- The packed E2E gate now always packs and installs the actual tarball and invokes its generated shim to prove missing-provenance no-write safety. The official pinned-source dry-run/install/repeat branch remains an explicit required direct gate because it requires the external pinned checkout and Python dependencies; it passed on this snapshot.
- README, packaged offline guide, and rendered docs describe the same Git-root, link-rejection, pinned-source environment, no-PATH-trust, and recovery-path contract.

## Tests

### Correction RED → GREEN

- RED: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest src/skills-install.test.ts --runInBand --coverage=false` exited `1`: 3 new safety cases failed and 6 passed. The arbitrary `.git` and `.agents` junction cases incorrectly installed, and restoration failure returned `SKILLS_INSTALL_FAILED` instead of preserving/reporting recovery data.
- GREEN: after the smallest Git discovery, redirected-path, and recoverable-transaction changes plus real-Git fixture correction, the same command exited `0`: 9 tests passed. One intermediate run exposed only Windows short/long path representation in the assertion; the assertion was corrected to compare the canonical real path.
- RED: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest src/skills-install.test.ts --runInBand --coverage=false --testNamePattern "validator source"` exited `1`: expected `SKILLS_VALIDATOR_PROVENANCE`, received `SKILLS_VALIDATOR_UNAVAILABLE` from the old unqualified PATH behavior.
- GREEN: the same provenance command exited `0`: 1 test passed, 9 skipped by the focused name filter.
- RED: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest src/docs.test.ts --runInBand --coverage=false --testNamePattern "official preflight"` exited `1` because the old docs omitted `JZ_TRELLO_FLOW_SKILLS_REF_CHECKOUT` and the new safety/recovery contract.
- GREEN: the same docs command exited `0`: 1 test passed, 7 skipped by the focused name filter.
- Focused aggregate: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest src/skills-install.test.ts src/cli.test.ts src/docs.test.ts test/skills-install.e2e.test.ts --runInBand --coverage=false` exited `0`: 4 suites passed, 76 tests passed, and only the external official-provenance success branch skipped; the mandatory packed gate ran.
- Direct official packed gate: `$env:JZ_TRELLO_FLOW_SKILLS_REF_CHECKOUT='C:\Temp\asrepo2'; $env:JZ_TRELLO_FLOW_SKILLS_REF_PYTHON='C:\Temp\asref\.venv\Scripts\python.exe'; pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest test/skills-install.e2e.test.ts --runInBand --coverage=false` exited `0`: 2 tests passed, including mandatory pack/install/missing-provenance safety and official pinned-source dry-run/install/repeat through the generated shim.

### Package and repository gates

- `pnpm --filter @jz/ai-arsenal-trello-work-cli format` exited `0`; Prettier reported all matched files use its style. The first pre-format check had exited `1` and identified the four newly edited TypeScript test/source files; `prettier --write` formatted exactly those files before this passing rerun.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli lint` exited `0`.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck` exited `0`.
- With the two pinned-source environment variables above, `pnpm --filter @jz/ai-arsenal-trello-work-cli test` exited `0`: 22 suites passed, 265 tests passed, and 2 credential-gated live Trello tests skipped.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli validate` exited `0`; strict publint reported `All good!`.
- Pinned identity/conformance command verified clean checkout HEAD `38a2ff82958afee88dadf4831509e6f7e9d8ef4e`, reported version `0.1.0`, and exited `0` after eight `Valid skill` results for four canonical and four bundled directories.
- Explicit `pnpm ... pack --pack-destination <unique temp>` produced a 32-file tarball at `C:\Users\jimzord12\AppData\Local\Temp\jz-trello-flow-pack-verify-115752381d7d460bbcf622581e1b02ef\jz-ai-arsenal-trello-work-cli-0.3.0.tgz`, 66,831 bytes, SHA-256 `cf6bb39e73f4b6065a71c4416115cf30b04a05f5f1f2d61b8a6137812cf8b391`. The composite inspection command exited `1` only because MSYS `/usr/bin/tar` parsed the Windows drive prefix as a remote host; `C:\Windows\System32\tar.exe -tf <same tarball>` then exited `0` and listed the intended 32-file boundary.
- With the pinned-source environment variables set, `pnpm check` exited `0`: root format, all three package lint/typecheck/test tasks, 36 workflow tests, living-workflow validation, and current work-item validation passed. Turbo did not propagate the custom validator variables, so its package task ran the mandatory packed gate and skipped the external official success branch; the required direct official packed command above passed on the same bytes.
- `git diff --check` exited `0` with no output.
- `node scripts/validate-living-workflow.mjs` exited `0` (`NEXT.md` 409 words; 17 required files before this report).
- Pre-report `node scripts/validate-monorepo-work-item.mjs --work-item 2026-07-29-jz-trello-flow-skills-install --json` exited `0` with `valid: true` and `nextSkill: "implement-monorepo-change"`.
- Changed-path and safety audit found only contract/plan paths plus required recovery artifacts. No canonical skill source, Trello credential/access, live Trello mutation, publication, version/Changeset/changelog, global installation, reconciliation, commit, or push occurred.

## Deviations

None.
