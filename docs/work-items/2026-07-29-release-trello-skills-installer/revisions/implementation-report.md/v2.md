Work item: 2026-07-29-release-trello-skills-installer
Artifact: implementation
Revision: 2
Prerequisites: contract@2,plan@2,approval@2
Status: superseded

# Changed paths

- `package.json`
- `packages/trello-work-cli/package.json`
- `packages/trello-work-cli/CHANGELOG.md`
- `scripts/verify-trello-skills-install-bytes.mjs`
- `scripts/verify-trello-skills-install-bytes.test.mjs`
- `docs/work-items/2026-07-29-release-trello-skills-installer/implementation-report.md`
- `docs/work-items/2026-07-29-release-trello-skills-installer/revisions/implementation-report.md/v1.md`
- `docs/work-items/2026-07-29-release-trello-skills-installer/revisions/verification.md/v1.md`
- `NEXT.md`

## Decisions and repaired findings

- Preserved failed `verification@1` and `implementation@1` as byte-identical historical artifacts except for the required `Status: superseded` header change before creating this report.
- Hardened the checked-in byte-proof command so declared payload/repository roots must be real directories, the sentinel must resolve to a regular file inside the repository root, every payload and managed file is checked after `realpath`, all managed skill roots and enumerated children remain contained, symlinks/junctions and unsupported entries are rejected, and manifest input/output cannot redirect through its parent or target.
- Added adversarial regression tests for a junction-redirected managed root, an escaping sentinel, and a directory used as an invalid sentinel. Existing transformed-byte, repeat-manifest, mismatch, extra-file, and changed-sentinel tests remain green.
- Reconciled `NEXT.md` to say exactly that the corrected `0.4.0` candidate is locally green while review, repository reconciliation, release-SHA CI, global installation, post-install truth, and final CI remain pending.
- No global installation, registry publication, Trello access, or non-disposable skill installation occurred.

## Durable exact Changesets status evidence

The pre-application candidate used this exact pending Changeset:

```markdown
---
'@jz/ai-arsenal-trello-work-cli': minor
---

Release the additive offline managed Trello Agent Skills installer and its packed payload for exact local distribution.
```

Exact command run before `pnpm version-packages`:

```text
pnpm changeset status --output "$TEMP/trello-0.4.0-status.json"
```

Exact JSON result (exit `0`):

```json
{
  "changesets": [
    {
      "releases": [
        {
          "name": "@jz/ai-arsenal-trello-work-cli",
          "type": "minor"
        }
      ],
      "summary": "Release the additive offline managed Trello Agent Skills installer and its packed payload for exact local distribution.",
      "id": "release-trello-skills-installer"
    }
  ],
  "releases": [
    {
      "name": "@jz/ai-arsenal-trello-work-cli",
      "type": "minor",
      "oldVersion": "0.3.0",
      "changesets": ["release-trello-skills-installer"],
      "newVersion": "0.4.0"
    }
  ]
}
```

`pnpm version-packages` then consumed `.changeset/release-trello-skills-installer.md`. The resulting diff changes only `packages/trello-work-cli/package.json` from `0.3.0` to `0.4.0` and adds the generated `0.4.0` Minor Changes section to its changelog; `pnpm-lock.yaml` and unrelated package versions are unchanged. A detached `HEAD` worktree replay with the exact Changeset and installed Changesets CLI reproduced the JSON above.

## Tests

- `node --test scripts/verify-trello-skills-install-bytes.test.mjs` — 5 passed; includes redirected managed-root and escaping/invalid sentinel adversarial cases.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath test/skills-install.e2e.test.ts` — 1 suite, 2 tests passed against the actual packed candidate and pinned official validator.
- Package format, lint, typecheck, complete test, and strict validation — passed; 22 suites, 266 tests passed and 2 live tests skipped in the direct enabled package run; strict publint passed.
- `pnpm check` — passed; all package gates and 41 workflow/byte-proof tests passed.
- `pnpm validate:workflow` — passed.
- `git diff --check` — passed.

## Deviations

None.
