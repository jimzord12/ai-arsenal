Work item: 2026-07-29-release-trello-skills-installer
Artifact: contract
Revision: 1
Prerequisites: request@1,context@1
Status: superseded

# Goal

Produce and install an exact, CI-green private `0.3.1` patch release containing the already verified Trello Agent Skills installer, and preserve durable exact-SHA, tarball, generated-shim, installed-payload, and final-CI evidence without registry publication or Trello access.

## Non-goals

- Registry publication or automated publication configuration.
- Any Trello API call or Trello mutation.
- Product behavior changes beyond generated version/changelog metadata and evidence/current-truth reconciliation.
- Installation into a non-disposable consumer repository.
- Manual editing of pnpm-generated global shims.
- Source deletion, history rewriting, or unrelated package changes.

## Hard walls

- Do not publish or call Trello.
- Use Changesets/SemVer and `pnpm version-packages`; do not hand-edit package version or changelog.
- Pack and globally install only a clean exact release commit after its required Quality and Portability workflows succeed for the full SHA.
- Use the actual tarball in an unrelated disposable Git repository for packed-consumer and installed `skills install` checks; do not substitute workspace source or mocks.
- Replace only the Windows global pnpm package from that tarball; do not globally link a mutable worktree or manually patch generated shims.
- Verify package identity/version, generated shim help/docs, installer execution, unrelated-skill preservation, and deterministic installed bytes without credentials.
- Record checksum and durable evidence, reconcile current truth after operational installation, push required documentation-only reconciliation, and require final exact-SHA CI.
- Finish with local `master == origin/master` and a clean worktree.

## Acceptance criteria

1. One patch Changeset selects only `@jz/ai-arsenal-trello-work-cli`; status predicts `0.3.0 -> 0.3.1`, `pnpm version-packages` consumes it, and generated package/changelog metadata report `0.3.1` with no unrelated package bump.
2. Focused installer tests, complete package format/lint/typecheck/test/strict validation, exact packed-consumer validation, root checks, workflow validators, and `git diff --check` pass without Trello access.
3. Fresh independent review of stable release bytes reports no Critical, High, or Medium finding; any such finding is repaired, invalidated gates rerun, and the corrected snapshot re-reviewed.
4. The reviewed release snapshot is committed and pushed, local/remote full SHAs match, and exact-SHA Quality and Portability workflows complete successfully.
5. A tarball packed only from that clean CI-green SHA has recorded file identity and SHA-256; its real manifest reports `0.3.1`, only `jz-trello-flow -> src/bin.ts`, all four managed skill payloads and protocol authority, and excludes tests/configuration/credentials/retired alias.
6. Native Windows pnpm replaces the global package from the exact tarball; global listing and installed manifest report exactly `@jz/ai-arsenal-trello-work-cli@0.3.1`; generated native shim `jz-trello-flow --help` and `docs` succeed without repository-source resolution.
7. In a disposable Git repository outside the source tree, the installed global shim performs `skills install`; four expected managed skill directories are present, unrelated sentinel bytes remain unchanged, repeated installation is deterministic, and installed files byte-match the expected deterministic package payload including documented managed-marker/protocol-link transformation.
8. Durable work-item and current-truth evidence records release/final SHAs, CI run IDs/URLs, tarball path/filename and SHA-256, package/file boundary, install command and version proof, shim help/docs, disposable-repository byte proof, and explicit no-publication/no-Trello compliance.
9. Any post-install current-truth reconciliation is committed/pushed and exact-SHA Quality and Portability CI pass; final `master == origin/master` and status is clean.

## Test seams

- Changesets status JSON, consumed changeset, generated manifest and changelog diff.
- Package installer unit/E2E suites and official pinned Agent Skills validation path.
- Real `pnpm pack` archive manifest/package metadata and install into an unrelated disposable consumer.
- GitHub Actions runs filtered by full commit SHA for Quality and Portability.
- Native Windows global pnpm registration and generated `.cmd` executable invocation.
- Disposable Git repository with sentinel unrelated skill, pre/post file hashes, repeated install, and transformed source-to-installed byte comparison.
- Git full-SHA equality and clean-worktree checks before packing and at final completion.

## Verification

- SemVer/Changesets: inspect status JSON before application and direct metadata/changelog/diff after application.
- Package and repository quality: run official focused/package/root/workflow commands and preserve exit codes/test totals.
- Distribution: pack the real candidate, inspect archive and packed `package.json`, install that exact tarball into a clean consumer, and invoke it.
- Independent review: inspect exact stable diff/full files and acceptance matrix; require zero Critical/High/Medium.
- Git/CI: push exact attributable commits, fetch, compare full SHAs, and require successful Quality and Portability runs bound to each required SHA.
- Operational installation: record tarball SHA-256, install via native Windows pnpm, inspect global registration/manifest/shims, run help/docs, and exercise disposable `skills install` with byte hashes.
- Final state: rerun workflow validators, `git diff --check`, full SHA equality, and clean status; record prohibited effects were not performed.

## Approval required

No. The direct user request and `AGENTS.md` autonomy policy authorize this bounded recoverable release, global replacement, commit/push, and CI chain; no dangerous deletion or irreversible data loss is planned.

## Authority classification

Dangerous deletion or irreversible data loss: `no`
Hard prerequisites: `resolved`
