Work item: 2026-07-29-release-trello-skills-installer
Artifact: plan
Revision: 1
Prerequisites: contract@1
Status: superseded

# Preconditions

- Contract revision 1 is ready, classifies dangerous deletion as `no`, and reports hard prerequisites resolved.
- Baseline was clean and aligned at `adda9b6e7515d5ada6af173fed20b7f33f055916`; package/global version is `0.3.0`; pnpm, Bun, GitHub CLI authorization, and rollback through the existing global `0.3.0` package are available.
- `AGENTS.md` mandates Changesets, reviewed exact-SHA CI, tarball global installation, generated-shim smoke, durable evidence, and clean aligned completion.
- Registry publication and every Trello call remain prohibited throughout.

## Ordered tasks

### 1. Apply the patch release metadata

- Paths: `.changeset/<generated>.md`, `packages/trello-work-cli/package.json`, `packages/trello-work-cli/CHANGELOG.md`, `pnpm-lock.yaml`
- Inputs: Acceptance criterion 1; current `0.3.0`; installer source delivery `251d0ff`; Changesets policy.
- Output: One pre-application patch Changeset predicts only `0.3.1`, then is consumed by `pnpm version-packages`; generated manifest/changelog identify `0.3.1` and no unrelated bump.
- Test command: `pnpm changeset status --output <disposable-status.json> && pnpm version-packages && git diff -- packages/trello-work-cli/package.json packages/trello-work-cli/CHANGELOG.md pnpm-lock.yaml`
- Expected result: Status JSON names only the Trello package with patch `0.3.0 -> 0.3.1`; application consumes the file and generated metadata is scoped.
- Rollback: Restore release metadata and the pending Changeset from the pre-task worktree if application is invalid.

### 2. Validate package, repository, and exact packed consumer

- Paths: `packages/trello-work-cli/**`, `package.json`, `scripts/**`, `docs/work-items/2026-07-29-release-trello-skills-installer/implementation-report.md`, `NEXT.md`
- Inputs: Acceptance criteria 2 and 5; package scripts; pinned official validator; real tarball and unrelated disposable consumer seams.
- Output: Focused installer, complete package, strict package, root/workflow, whitespace, real archive, clean-consumer install/invocation, and payload byte checks pass; implementation evidence is recorded and routed to verification.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli format && pnpm --filter @jz/ai-arsenal-trello-work-cli lint && pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck && pnpm --filter @jz/ai-arsenal-trello-work-cli test && pnpm --filter @jz/ai-arsenal-trello-work-cli validate && pnpm check && pnpm validate:workflow && git diff --check`
- Expected result: Every command exits zero; actual tarball/consumer exposes the exact intended package, executable, skills, and deterministic installation boundary without credentials or Trello calls.
- Rollback: Remove only disposable archives/consumers; preserve truthful failure evidence and revert release metadata only if the approved release cannot be validated.

### 3. Independently verify stable release bytes

- Paths: `docs/work-items/2026-07-29-release-trello-skills-installer/verification.md`, `NEXT.md`
- Inputs: Acceptance criterion 3; stable diff, full changed files, test outputs, exact packed-consumer evidence.
- Output: Fresh independent review and verification matrix report no Critical/High/Medium, or route failures to approved repair and repeat review after the final normative edit.
- Test command: `git status --short && git diff --check && node scripts/validate-monorepo-work-item.mjs --work-item 2026-07-29-release-trello-skills-installer --json`
- Expected result: Changed paths match the plan, whitespace is clean, review has no blocking severity, and validator routes passed verification to reconciliation.
- Rollback: Not applicable.

### 4. Reconcile, commit, push, and verify release CI

- Paths: `NEXT.md`, `docs/planning/CANONICAL_IMPLEMENTATION_PLAN.md`, `docs/work-items/2026-07-29-release-trello-skills-installer/reconciliation.md`
- Inputs: Acceptance criteria 4 and 8; passed verification; current-truth rules.
- Output: Release-pending current truth is reconciled, exact snapshot committed/pushed, full local/remote SHAs equal, and Quality/Portability succeed for the release SHA.
- Test command: `pnpm validate:workflow && git diff --check && git push origin master && git fetch origin && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/master)" && gh run list --commit "$(git rev-parse HEAD)"`
- Expected result: Validators and Git checks pass; both named workflows complete successfully for the full release SHA.
- Rollback: Before push, restore planning changes; after push, retain the valid private release commit and do not globally install if CI fails.

### 5. Pack the clean CI-green SHA and replace the global package

- Paths: `packages/trello-work-cli/package.json`, `packages/trello-work-cli/assets/agent-skills/**`, `packages/trello-work-cli/src/bin.ts`, `<disposable-artifact-directory>`, Windows global pnpm store/shims
- Inputs: Acceptance criteria 5 and 6; exact release SHA; successful exact-SHA CI; existing `0.3.0` registration as rollback.
- Output: Exact clean release tarball and SHA-256 are recorded; archive boundary is inspected; native Windows pnpm installs it globally; installed manifest/listing and generated shim help/docs prove `0.3.1`.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli pack --pack-destination <disposable-artifact-directory> && sha256sum <tarball> && cmd.exe /d /c pnpm add -g <native-tarball-path> && cmd.exe /d /c pnpm list -g --depth=0 && cmd.exe /d /c jz-trello-flow --help && cmd.exe /d /c jz-trello-flow docs`
- Expected result: Tarball identity/boundary matches acceptance; native generated shim executes installed `0.3.1` without source-tree resolution.
- Rollback: Reinstall the previously working exact `0.3.0` package if replacement or smoke fails; never delete source or manually edit shims.

### 6. Verify installed skills and record post-install truth

- Paths: `<disposable-git-repository>/.agents/skills/**`, `docs/work-items/2026-07-29-release-trello-skills-installer/reconciliation.md`, `NEXT.md`, `docs/planning/CANONICAL_IMPLEMENTATION_PLAN.md`
- Inputs: Acceptance criteria 7-9; globally installed shim; expected deterministic payload transformation; release/install evidence.
- Output: Installed global shim creates exactly four managed skill directories, preserves an unrelated sentinel, repeats deterministically, and byte checks match expected transformed package payload; durable evidence is updated and final docs commit/push/CI completes.
- Test command: `cmd.exe /d /c jz-trello-flow skills install --repo <disposable-git-repository> && <byte-comparison-script> && pnpm validate:workflow && git diff --check && git push origin master && git fetch origin && gh run list --commit "$(git rev-parse HEAD)"`
- Expected result: All byte/sentinel/repeat checks pass, both final workflows succeed for the exact final SHA, local and remote master match, and the worktree is clean.
- Rollback: Remove only the disposable repository/artifacts; if documentation CI fails, repair within current truth and repeat the final reconciliation CI loop.

## Affected paths

- Create: `.changeset/<generated>.md` (consumed by Changesets), `docs/work-items/2026-07-29-release-trello-skills-installer/{request,context,change-contract,implementation-plan,approval,implementation-report,verification,reconciliation}.md`.
- Modify: `packages/trello-work-cli/package.json`, `packages/trello-work-cli/CHANGELOG.md`, `pnpm-lock.yaml` only if Changesets generates a real lockfile change, `NEXT.md`, `docs/planning/CANONICAL_IMPLEMENTATION_PLAN.md`.
- Delete: none, except the pending Changeset consumed by the official version command and isolated disposable artifacts after evidence capture.

## Verification commands

- `pnpm changeset status --output <disposable-status.json>` — predicts only patch `0.3.1`.
- `pnpm version-packages` — consumes the Changeset and generates package/changelog metadata.
- Package focused/full format, lint, typecheck, test, strict publint and actual packed-consumer commands — all exit zero without credentials.
- `pnpm check && pnpm validate:workflow && git diff --check` — root/workflow/whitespace gates pass.
- Fresh independent exact-snapshot review — zero Critical/High/Medium.
- `git fetch origin` plus full SHA comparisons and exact-commit `gh run` inspection — required release and final Quality/Portability runs succeed.
- Exact clean-SHA pack, `sha256sum`, archive inspection, native `pnpm add -g`, global manifest/listing, generated `--help` and `docs` — exact installed `0.3.1` works.
- Disposable Git-repository installed-shim invocation plus deterministic hash comparison and unrelated sentinel assertion — exact four managed skills match expected bytes on first/repeated installs.
- Final workflow validation/full SHA equality/clean status — all pass.

## Rollback

1. Before commit, restore generated release/planning metadata if mandatory validation cannot pass.
2. After a valid pushed release but failed CI, do not install globally; repair and create a new exact reviewed SHA.
3. After failed global replacement/smoke, reinstall the previous exact `0.3.0` package and preserve failure evidence.
4. Remove only disposable archives and disposable repositories after evidence capture; do not publish, call Trello, delete source, rewrite history, or manually modify generated shims.
