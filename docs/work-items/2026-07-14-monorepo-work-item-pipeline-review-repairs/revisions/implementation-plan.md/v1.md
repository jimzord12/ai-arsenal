Work item: 2026-07-14-monorepo-work-item-pipeline-review-repairs
Artifact: plan
Revision: 1
Prerequisites: contract@1
Status: superseded

# Preconditions

- `request.md`, `context.md`, and `change-contract.md` are ready revision 1
  artifacts for this work item.
- `NEXT.md` names this work item and awaits the required approval record at
  `record-monorepo-approval`.
- The user-approved Monorepo Work-Item Pipeline design and Task 13 review
  scope remain authoritative; the existing worktree changes, unrelated
  feature-resume planning artifacts, source rollback copy, and consumer state
  must be preserved.
- Implementation remains blocked until the user explicitly approves this exact
  plan and `record-monorepo-approval` records its SHA-256 digest.

## Ordered tasks

### 1. Reject an absent registration and validate the current registered route

- Paths: `scripts/validate-monorepo-work-item.test.mjs`, `scripts/validate-monorepo-work-item.mjs`, `package.json`
- Inputs: Acceptance criteria 1–2 and 7; the `parseActiveState`, `--work-item none`, and current-registration seams; temporary work-item fixtures.
- Output: Child-process tests prove that absent and partial active-registration blocks fail, while one explicit `none` pair remains valid. A portable `--current` mode validates the explicit inactive state or the active work-item selected by `NEXT.md`, and the root workflow command uses that mode.
- Test command: `node --test scripts/validate-monorepo-work-item.test.mjs`
- Expected result: The new missing-registration and current-registration cases fail before the parser and argument-handling change. The focused suite exits `0` after it, with JSON `valid: false` and a nonempty blocker for invalid registration, while `pnpm validate:workflow` succeeds for the active work item without platform-specific shell logic.
- Rollback: Not applicable.

### 2. Characterize revisioned contract and plan routing in disposable fixtures

- Paths: `scripts/validate-monorepo-work-item.test.mjs`
- Inputs: Acceptance criteria 3–6; the normative archive-plus-increment contract; existing fixture helpers for archived artifacts.
- Output: Temporary-root cases prove archive headers, incremented contract and plan revisions, exact prerequisite references, reverse-order downstream archival, plan routing after a contract revision, and the fresh-approval stop after a plan revision.
- Test command: `node --test scripts/validate-monorepo-work-item.test.mjs`
- Expected result: The focused suite exits `0`; no real `docs/work-items/` artifact other than this active work item and no consumer `.scratch` state is created or mutated.
- Rollback: Not applicable.

### 3. Make contract and plan revision recovery executable and guarded

- Paths: `.agents/skills/scope-monorepo-change/SKILL.md`, `.agents/skills/plan-monorepo-change/SKILL.md`, `docs/workflow/MONOREPO_WORK_ITEM_PIPELINE.md`, `scripts/validate-living-workflow.mjs`, `scripts/validate-living-workflow.test.mjs`
- Inputs: Acceptance criteria 3–6; the approved design's artifact-writing boundaries; Task 13's concrete review finding; revision-fixture evidence from Task 2.
- Output: Scoping archives and increments only the contract it replaces, archives invalidated downstream current artifacts in reverse dependency order, removes their stale current copies, and hands off to planning; planning performs the corresponding plan revision, invalidation, and fresh-approval handoff. The normative workflow document describes the current-registration command, and the disposable workflow validator guards both that command and the stage responsibilities.
- Test command: `node --test scripts/validate-living-workflow.test.mjs`
- Expected result: A fixture lacking either stage's revision-recovery rule fails before the living-workflow validator change; the focused suite and `node scripts/validate-living-workflow.mjs` exit `0` once the guarded instructions and normative documentation agree.
- Rollback: Not applicable.

### 4. Re-run the Task 13 review and verification gates

- Paths: `.superpowers/sdd/task-13-report.md`, `docs/evidence/maintenance-monorepo-work-item-pipeline/task-13-wide-integration-review.md`
- Inputs: Acceptance criteria 7; all repaired workflow paths; Task 13's three required independent review lenses.
- Output: A durable Task 13 review record maps every concrete finding to its repair and records the final independent-review verdicts and verification evidence.
- Test command: `pnpm check`
- Expected result: `pnpm check`, `pnpm validate:workflow`, and `git diff --check` each exit `0`; `git status --short` contains only intentional pipeline changes, this active work item, Task 13 evidence, and separately approved preflight artifacts.
- Rollback: Not applicable.

## Affected paths

- Modify: `scripts/validate-monorepo-work-item.mjs`
- Modify: `scripts/validate-monorepo-work-item.test.mjs`
- Modify: `package.json`
- Modify: `.agents/skills/scope-monorepo-change/SKILL.md`
- Modify: `.agents/skills/plan-monorepo-change/SKILL.md`
- Modify: `docs/workflow/MONOREPO_WORK_ITEM_PIPELINE.md`
- Modify: `scripts/validate-living-workflow.mjs`
- Modify: `scripts/validate-living-workflow.test.mjs`
- Create: `.superpowers/sdd/task-13-report.md`
- Create: `docs/evidence/maintenance-monorepo-work-item-pipeline/task-13-wide-integration-review.md`

## Verification commands

- `node --test scripts/validate-monorepo-work-item.test.mjs` — focused
  validator and disposable revision-fixture coverage exits `0`.
- `node --test scripts/validate-living-workflow.test.mjs` — focused workflow
  policy fixtures exit `0`.
- `pnpm test:workflow` — both workflow validator suites exit `0`.
- `node scripts/validate-living-workflow.mjs` — living-workflow structure and
  required revision ownership pass.
- `node scripts/validate-monorepo-work-item.mjs --work-item 2026-07-14-monorepo-work-item-pipeline-review-repairs --json` — the active work-item
  route validates at each stage.
- `pnpm check` — formatting, linting, typechecking, package tests, workflow
  tests, and current-work-item validation exit `0` while the work item is
  active.
- `pnpm validate:workflow` — both root workflow validators exit `0` for the
  active work item and again after reconciliation clears the registration.
- `git diff --check` — no whitespace errors.

## Rollback

Not applicable. The implementation changes only workflow validation, isolated
fixtures, skill instructions, and documentation. No package, user data,
consumer state, distribution, or source-copy state is changed.
