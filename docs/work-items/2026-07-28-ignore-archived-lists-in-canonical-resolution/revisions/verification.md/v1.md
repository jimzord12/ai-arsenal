Work item: 2026-07-28-ignore-archived-lists-in-canonical-resolution
Artifact: verification
Revision: 1
Prerequisites: contract@1,plan@1,implementation@1
Status: superseded

# Commands

- `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/board.test.ts src/list-management.test.ts src/cli.test.ts src/transition.test.ts` — focused acceptance and consumer suites.
- `pnpm check` — root formatting, lint, typecheck, package tests, workflow tests, and workflow validation.
- `node scripts/validate-monorepo-work-item.test.mjs` — work-item pipeline suite.
- `node scripts/validate-living-plan.mjs` — exact approved-plan command.
- `node scripts/validate-living-workflow.mjs` — repository's actual living-workflow validator.
- `git diff --check` — whitespace validation.
- Independent fresh-agent review of the exact current diff against contract and plan.
- Manual changed-path comparison using `git status --short`, `git diff --name-only`, the implementation report, and approved affected paths.

## Exit codes

- Focused four-suite Jest command: `0` (4 suites, 93 tests passed).
- `pnpm check`: `0`.
- `node scripts/validate-monorepo-work-item.test.mjs`: `0` (19 tests passed in the direct run; the root workflow command also passed the complete 30-test combined suite).
- `node scripts/validate-living-plan.mjs`: `1` (`MODULE_NOT_FOUND`; the approved plan names a script absent from the repository).
- `node scripts/validate-living-workflow.mjs`: `0`.
- `git diff --check`: `0`.
- Independent review and manual observations: Not applicable.

## Observed result

- Open plus archived canonical namesake resolution, two-open ambiguity, archived override rejection, all-list visibility, wrong-board checks, and unchanged explicit-ID close/replay/audit paths were observed in code and tests.
- Root quality, complete package tests, focused tests, actual workflow validation, and whitespace checks passed.
- Independent review found no security, credential, Trello-mutation, installation, or unrelated-scope issue.
- Independent review blocked verification because no new public-boundary regression proves archived configured targets are rejected before card creation/design/transition mutation, and archived-only initialization is covered only in dry-run rather than normal execution.
- The approved plan's `validate-living-plan.mjs` command is not executable because that file does not exist; the repository-authoritative `validate-living-workflow.mjs` command passed.
- Changed implementation paths are within the approved affected-path set, but `cli.test.ts` and `transition.test.ts` planned regression changes are absent.

## Status

Failed.

## Remaining failures

- Add public-boundary no-mutation regression evidence for archived canonical targets before active card placement and transition dispatch.
- Add a normal-execution archived-only initialization regression proving creation of a new open canonical list.
- Reconcile the approved plan's nonexistent `validate-living-plan.mjs` command through the appropriate plan-revision stage before a later verification can satisfy every exact required command.
