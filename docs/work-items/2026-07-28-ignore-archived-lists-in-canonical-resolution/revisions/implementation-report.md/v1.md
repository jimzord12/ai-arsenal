Work item: 2026-07-28-ignore-archived-lists-in-canonical-resolution
Artifact: implementation
Revision: 1
Prerequisites: contract@1,plan@1,approval@1
Status: superseded

# Changed paths

- `packages/trello-work-cli/src/board.ts`
- `packages/trello-work-cli/src/board.test.ts`
- `packages/trello-work-cli/src/list-management.ts`
- `packages/trello-work-cli/src/list-management.test.ts`
- `docs/work-items/2026-07-28-ignore-archived-lists-in-canonical-resolution/implementation-report.md`
- `NEXT.md` (pipeline-step handoff only)

## Decisions

- Kept `TrelloClient.listBoardLists` and `listManagedLists` unchanged so archived lists remain visible for explicit reads, audit, cleanup, and recovery.
- Filtered `closed` lists only at active canonical name-resolution and guarded workflow-initialization seams.
- Rejected archived configured canonical list IDs with stable `WORKFLOW_LIST_CLOSED` recovery evidence before active card placement or transition handlers can receive the mapping.
- Preserved ordinary explicit list creation's existing duplicate-name behavior. Only workflow-init child creation may coexist with an archived namesake, identified by its existing parent operation record.
- Used the mapping and initialization production seams as the narrow enforcement point. Existing CLI and transition suites exercise their public consumers and remained green without duplicating closed-state knowledge inside `transitionWorkUnit`.

## Tests

- RED: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/board.test.ts src/list-management.test.ts` failed as expected: open plus archived `Done` was ambiguous, an archived configured mapping was accepted, and archived-only `Inbox` was preserved instead of reported missing. Result: 3 failed, 31 passed.
- GREEN: the same focused command passed after implementation. Result: 2 suites, 34 tests passed.
- Public consumer regression sweep: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/board.test.ts src/list-management.test.ts src/cli.test.ts src/transition.test.ts`. Result: 4 suites, 93 tests passed.
- Initial package gate exposed formatting-only drift in `src/board.ts` and `src/list-management.test.ts`; no later gate ran in that chained command.
- Applied Prettier to the four touched source/test files, then reran package gates.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli format`: passed.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli lint`: passed.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck`: passed.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli test`: passed, 19 suites with 233 passed and 2 live cases skipped.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli validate`: strict publint passed.
- No test loaded live credentials or mutated Trello.

## Deviations

None.
