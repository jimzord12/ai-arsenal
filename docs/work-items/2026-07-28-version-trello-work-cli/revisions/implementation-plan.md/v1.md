Work item: 2026-07-28-version-trello-work-cli
Artifact: plan
Revision: 1
Prerequisites: contract@1
Status: superseded

# Preconditions

- `contract@1` is current and ready; `request@1` and `context@1` remain current.
- `HEAD`, `master`, and `origin/master` start equal at `dda2438f4946f72c0e161aa64e39dc972ff4966b`; preserve unrelated work if the snapshot changes.
- Root `AGENTS.md`, `NEXT.md`, canonical-plan section 24, the work-item pipeline, and package metadata/Changesets configuration remain applicable.
- Version `0.1.0` is user-selected. Publication and Trello mutation remain prohibited.
- Product changes require a digest-bound approval of this exact plan. Global installation remains outside implementation/verification/reconciliation and may run only after the exact versioned commit is pushed and required CI is green.

## Ordered tasks

### 1. Create the Trello CLI minor changeset

- Paths: `.changeset/trello-work-cli-0-1-0.md`
- Inputs: The `0.1.0` user decision, archived-list fix at `dda2438`, configured Changesets changelog generation, and the acceptance criterion that no unrelated package is versioned.
- Output: One pending minor Changeset for `@jz/ai-arsenal-trello-work-cli` describing open-only active canonical resolution, archived-only initialization, and rejection of archived configured targets.
- Test command: `pnpm changeset status --output .tmp/trello-work-cli-changeset-status.json`
- Expected result: Exactly one release is reported, for `@jz/ai-arsenal-trello-work-cli` at `0.1.0`; no other package release is present. Remove the disposable `.tmp` output immediately after inspection.
- Rollback: Remove only `.changeset/trello-work-cli-0-1-0.md` before version application.

### 2. Apply the Changesets version and changelog generation

- Paths: `.changeset/trello-work-cli-0-1-0.md`, `packages/trello-work-cli/package.json`, `packages/trello-work-cli/CHANGELOG.md`, `pnpm-lock.yaml`
- Inputs: The validated single-package minor Changeset and configured `pnpm version-packages` command.
- Output: Changesets consumes the pending file, writes package version `0.1.0`, creates the generated changelog entry, and updates only required lockfile metadata if Changesets/pnpm changes it.
- Test command: `pnpm version-packages && pnpm changeset status`
- Expected result: The changeset is consumed, the Trello package/changelog report `0.1.0`, no pending releases remain, and no unrelated package version changes.
- Rollback: Restore only these task-owned metadata paths to the captured base before Git delivery; do not rewrite shared history.

### 3. Validate package identity and repository quality

- Paths: `packages/trello-work-cli/package.json`, `packages/trello-work-cli/CHANGELOG.md`, `pnpm-lock.yaml`
- Inputs: Versioned metadata, generated changelog, existing package scripts, root gates, and exact-artifact acceptance criteria.
- Output: Verified `@jz/ai-arsenal-trello-work-cli@0.1.0` package metadata and packed artifact, with package/root/workflow gates passing.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli format && pnpm --filter @jz/ai-arsenal-trello-work-cli lint && pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck && pnpm --filter @jz/ai-arsenal-trello-work-cli test && pnpm --filter @jz/ai-arsenal-trello-work-cli validate && pnpm check && node scripts/validate-monorepo-work-item.test.mjs && node scripts/validate-living-workflow.mjs && git diff --check`
- Expected result: Package identity is `0.1.0`; all package, root, 30-test workflow, living-workflow, and whitespace gates pass without publication, global installation, or Trello access.
- Rollback: Not applicable.

### 4. Independently verify and reconcile the exact snapshot

- Paths: `docs/work-items/2026-07-28-version-trello-work-cli/implementation-report.md`, `docs/work-items/2026-07-28-version-trello-work-cli/verification.md`, `docs/work-items/2026-07-28-version-trello-work-cli/reconciliation.md`, `docs/planning/CANONICAL_IMPLEMENTATION_PLAN.md`, `NEXT.md`
- Inputs: Exact version/changelog diff, package/root gates, fresh independent-review requirement, and post-CI installation boundary.
- Output: Passed verification/reconciliation proving the metadata and changelog are bounded, current planning truth identifies the exact `0.1.0` package, and the next operation is Git/CI capture followed by exact-package global installation.
- Test command: `node scripts/validate-monorepo-work-item.mjs --work-item 2026-07-28-version-trello-work-cli --json && node scripts/validate-living-workflow.mjs && git diff --check`
- Expected result: Independent review has no blocking finding; the completed work item validates and living-workflow/whitespace checks pass.
- Rollback: Not applicable.

### 5. Capture the exact repository artifact through Git and CI

- Paths: All task-owned paths listed below.
- Inputs: Passed current verification/reconciliation, implicit commit/push authority for required CI, and prohibition on installation before green CI.
- Output: One bounded conventional commit on `master`, pushed without history rewriting, contained in `origin/master`, with required Quality and Portability GitHub Actions passing for the exact SHA.
- Test command: `git diff --cached --check && git push origin master && env -u GH_TOKEN gh run list --branch master --limit 10 --json workflowName,status,conclusion,url,headSha`
- Expected result: `HEAD` equals `origin/master`; both required workflows conclude `success` for the exact commit.
- Rollback: Before push, reset only task-owned staged paths if delivery fails. After push, use a normal revert commit if required; never rewrite shared history.

## Affected paths

### Create

- `.changeset/trello-work-cli-0-1-0.md` (temporary, consumed by Changesets)
- `packages/trello-work-cli/CHANGELOG.md`
- `docs/work-items/2026-07-28-version-trello-work-cli/implementation-report.md`
- `docs/work-items/2026-07-28-version-trello-work-cli/verification.md`
- `docs/work-items/2026-07-28-version-trello-work-cli/reconciliation.md`

### Modify

- `packages/trello-work-cli/package.json`
- `pnpm-lock.yaml` only if Changesets/pnpm updates required workspace metadata
- `docs/planning/CANONICAL_IMPLEMENTATION_PLAN.md`
- `NEXT.md`

### Delete

- `.changeset/trello-work-cli-0-1-0.md` through `pnpm version-packages`

## Verification commands

- `pnpm changeset status --output .tmp/trello-work-cli-changeset-status.json` — exactly one Trello package minor release to `0.1.0` before application.
- `pnpm version-packages && pnpm changeset status` — Changesets generates version/changelog and leaves no pending release.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli format` — package formatting passes.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli lint` — package lint passes.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck` — package typecheck passes.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli test` — package tests pass.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli validate` — strict publint identifies and validates `0.1.0`.
- `pnpm check` — root aggregate gates pass.
- `node scripts/validate-monorepo-work-item.test.mjs` — workflow lifecycle tests pass.
- `node scripts/validate-living-workflow.mjs` — living workflow passes.
- `git diff --check` — no whitespace errors.
- `node scripts/validate-monorepo-work-item.mjs --work-item 2026-07-28-version-trello-work-cli --json` — stage and completion routes are valid.
- Git and GitHub CLI commands in task 5 — exact commit is remote and Quality/Portability CI are green.
- Post-completion operation: `pnpm add --global ./packages/trello-work-cli` from the exact CI-green commit, followed by `pnpm list --global --depth 0` and `work --help`; installation reports `@jz/ai-arsenal-trello-work-cli 0.1.0` and help exits successfully without Trello credentials.

## Rollback

- Before version application, remove only the pending task-owned Changeset.
- Before Git delivery, restore only task-owned metadata/planning/work-item paths to `dda2438` if validation fails.
- After push, use a normal revert commit if remote rollback is required; never rewrite `master` history.
- If post-completion global installation fails, reinstall the previously captured package or remove only `@jz/ai-arsenal-trello-work-cli` from the global pnpm environment; do not publish or mutate Trello.
