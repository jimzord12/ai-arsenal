Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: verification
Revision: 7
Prerequisites: contract@3,plan@5,implementation@8
Status: superseded

# Commands

| Criterion | Command / evidence | Result |
| --- | --- | --- |
| Authorized TestingBoard live E2E | Exact allowlisted command against board `6a16bbf1fea5389eb39636b7` | Exit 0; 11/11 passed. |
| Live postcondition inspection | Exact run-marker card/list queries | All four run cards in Done; all run-created disposable/collision-target lists closed. |
| Package/root/workflow gates | Sanitized package and root format/lint/typecheck/test plus 30 workflow tests and living-workflow validation | Exit 0; package 203 passed with 1 ordinary-run live skip. |
| Strict package validation | `publint --strict --pack pnpm`, immediate tarball removal | Exit 0; `tarballs=0`. |
| Independent exact-snapshot review | Read-only contract/plan/diff/source review | Failed with two High findings. |

## Observed result

The comprehensive live scenario and cleanup pass, but guarded initialization violates its no-existing-list-mutation boundary when the board is already complete. To durably record a parent workflow-init operation, `initializeBoardWorkflow` temporarily renames an existing canonical list with a marker and then restores it. Contract@3 permits initialization to create only missing canonical lists and forbids automatic changes to existing lists.

A safe distinction exists: when a prior initialization created missing canonical lists, its parent marker can be recorded in those child-create action names and later replay/collision can be recovered from them. When the board was already complete and no prior marker exists, initialization is a mutation-free verified no-op; it must not manufacture durable identity by renaming an existing list.

Plan@5 also requires an explicit task-path staged snapshot for final review. The index is currently empty, so cached checks are vacuous. Final verification must stage only approved task paths before reviewing the exact cached snapshot.

## Status

Failed.

## Remaining failures

- Remove the existing-list rename/write fallback for already-complete initialization.
- Preserve replay/collision recovery from markers attached to genuinely run-created missing canonical lists.
- Add regression coverage proving zero writes when initialization is already complete without a prior marker.
- Stage only plan-authorized task paths and rerun cached whitespace/path review plus live and independent verification.
