Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: verification
Revision: 1
Prerequisites: contract@2,plan@2,implementation@1
Status: superseded

# Commands

| Source / criterion | Command or observation | Actual result |
| --- | --- | --- |
| Workflow preflight | `node.exe scripts/validate-monorepo-work-item.mjs --current --json` | Exit 0; valid and routed to `verify-monorepo-change`. |
| Changed-path attribution | `git status --short`, implementation-report comparison, and approved-plan path comparison | Failed. `packages/trello-work-cli/src/docs.ts` and `packages/trello-work-cli/src/checklist.test.ts` changed but are absent from plan@2's affected-path inventory. The broad pre-registration `NEXT.md` content diff was recorded in context@1 as user-provided baseline; implementation changed only its pipeline field. |
| Focused offline contract suites | `env -u TRELLO_API_KEY -u TRELLO_API_TOKEN -u TRELLO_API_SECRET -u TRELLO_BOARD_ID -u TRELLO_LIVE_E2E pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/board.test.ts src/list-management.test.ts src/trello-client.test.ts src/config.test.ts src/cli.test.ts src/read-commands.test.ts src/mutation.test.ts src/docs.test.ts` | Exit 0; 8 suites and 97 tests passed. |
| Offline live preflight | `env -u TRELLO_API_KEY -u TRELLO_API_TOKEN -u TRELLO_API_SECRET -u TRELLO_BOARD_ID -u TRELLO_LIVE_E2E pnpm --filter @jz/ai-arsenal-trello-work-cli test:live` | Exit 0; 3 preflight tests passed and the live scenario was skipped. |
| Package format | `pnpm --filter @jz/ai-arsenal-trello-work-cli format` | Exit 0. |
| Package lint | `pnpm --filter @jz/ai-arsenal-trello-work-cli lint` | Exit 0. |
| Package typecheck | `pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck` | Exit 0. |
| Package tests | `env -u TRELLO_API_KEY -u TRELLO_API_TOKEN -u TRELLO_API_SECRET -u TRELLO_BOARD_ID -u TRELLO_LIVE_E2E pnpm --filter @jz/ai-arsenal-trello-work-cli test` | Exit 0; 18 suites passed, 185 tests passed, 1 live scenario skipped. |
| Root format/lint/typecheck/test | `env -u TRELLO_API_KEY -u TRELLO_API_TOKEN -u TRELLO_API_SECRET -u TRELLO_BOARD_ID -u TRELLO_LIVE_E2E pnpm format:check && pnpm lint && pnpm typecheck && pnpm test` | Exit 0; all three workspace packages passed. |
| Workflow tests | `pnpm test:workflow` | Exit 0; 30 tests passed. |
| Living workflow and whitespace | `node.exe scripts/validate-living-workflow.mjs && git diff --check && git diff --cached --check` | Exit 0. |
| Package validation | `pnpm --filter @jz/ai-arsenal-trello-work-cli validate` | Not run: configured validation packs the package, and packing is explicitly prohibited. This required plan command therefore lacks evidence. |
| Confirmed live E2E | `TRELLO_LIVE_E2E=1 pnpm --filter @jz/ai-arsenal-trello-work-cli test:live` | Not run because the exact testing-board and list identifiers remain unconfirmed. Contract-required live acceptance is unobserved. |
| Independent exact-snapshot review | Fresh independent reviewer inspection of contract@2, plan@2, implementation@1, all tracked diffs, and untracked files | Failed with High findings: incomplete contract-required live scenario coverage and missing create-name uniqueness; also identified changed paths outside plan. Medium finding: update replay fingerprint incorporates mutable inferred current state. |
| Live-harness acceptance coverage | Independent source/test inspection of `test/live-support.ts` and `test/live-trello.e2e.test.ts` | Failed. The live scenario omits unknown/ambiguous board names, wrong-board list, occupied configured-list close, stale version, replay/collision for every list mutation family, Work Unit replay, deterministic ambiguous/partial recovery, and list-mutation dry runs. |
| Unique list-name behavior | Independent source/test inspection of `src/list-management.ts` and `src/list-management.test.ts` | Failed. Create does not enumerate board lists and reject an existing exact open/closed list name before writing. |

## Exit codes

- Workflow preflight: `0`.
- Focused offline suites: `0`.
- Offline live preflight: `0` with live scenario skipped.
- Package format, lint, typecheck, and tests: `0`.
- Root format, lint, typecheck, tests, workflow tests, living-workflow validation, and whitespace checks: `0`.
- Package validation: Not run because it would pack.
- Confirmed live E2E: Not run because board/list identifiers are unconfirmed.
- Manual changed-path and independent-review observations: Not applicable.

## Observed result

The offline implementation is substantial and all executed deterministic gates pass. Inspection confirms stateless per-invocation board selection, exact-name failure behavior, read-only board listing, board/list ownership and occupancy checks before mutation, dry-run write suppression, recovery evidence, no board mutation API, no card/checklist archive/delete API, and status-only card cleanup with an empty-list close check.

Verification cannot pass because required live evidence is absent, the explicit live scenario does not cover all contract-required boundaries, unique list-name enforcement is missing, and two modified package paths fall outside plan@2's exact inventory. The reviewer’s `NEXT.md` scope concern is not counted as an implementation failure because context@1 preserved evidence that the broad `NEXT.md` rewrite predated capture; only pipeline registration fields changed during this work item.

## Status

Failed.

## Remaining failures

- Add contract-required live-scenario coverage for unknown/ambiguous board selectors, wrong-board list, occupied configured-list close, stale version, each list mutation family's dry-run/replay/collision behavior, Work Unit replay, and deterministic partial/ambiguous recovery where injectable.
- Enforce exact list-name uniqueness against open and closed lists before create mutation, with focused regression tests.
- Correct update replay identity so omitted mutable fields do not change the caller-request fingerprint; preserve postcondition-drift recovery semantics.
- Resolve the unauthorized-path mismatch for `src/docs.ts` and `src/checklist.test.ts` through a fresh approved plan revision or remove the changes without weakening the contract.
- Confirm exact dedicated testing board/list identifiers and run the opt-in live suite successfully; verify cards in Done, only empty run-created lists closed, and secret-free recovery evidence.
- Resolve the prohibited-pack conflict for the plan-required package validation command before claiming that evidence.
