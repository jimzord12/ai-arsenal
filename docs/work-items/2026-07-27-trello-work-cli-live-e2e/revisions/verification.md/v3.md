Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: verification
Revision: 3
Prerequisites: contract@3,plan@5,implementation@4
Status: superseded

# Commands

| Criterion | Command | Result |
| --- | --- | --- |
| Workflow preflight | `node.exe scripts/validate-monorepo-work-item.mjs --current --json` | Exit 0; valid verify route. |
| Sanitized typecheck/focused suites | `env -u TRELLO_API_KEY -u TRELLO_API_TOKEN -u TRELLO_API_SECRET -u TRELLO_BOARD_ID -u TRELLO_LIVE_E2E pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck` and focused 9-suite Jest command | Exit 0; 9 suites, 110 passed, 1 real-live scenario skipped. |
| TestingBoard workflow-init dry-run | `bun.exe packages/trello-work-cli/src/bin.ts workflow init --board TestingBoard --dry-run --operation-id live-e2e-preflight-20260728 --output json` | Exit 0; planned creation of Inbox, Ready, In Progress, Review, Blocked while preserving Done on board `6a16bbf1fea5389eb39636b7`. |
| Authorized live E2E | `TRELLO_LIVE_E2E=1 TRELLO_LIVE_CREDENTIAL_SOURCE=process-env TRELLO_LIVE_BOARD_SELECTOR=TestingBoard TRELLO_LIVE_BOARD_ID=6a16bbf1fea5389eb39636b7 pnpm --filter @jz/ai-arsenal-trello-work-cli test:live` | Exit 1; 5 tests passed and the real scenario failed. |
| Board-scoped recovery inspection | `work lists list --board TestingBoard --output json` and board-card query filtered by exact run marker | Canonical lists were created; the run-created disposable list was closed; one run card remained open in Inbox. No unrelated board was accessed or mutated. |
| Recovery transition attempt | Remove the run-only invalid Open Questions section, then invoke board-scoped CLI transitions with run-specific operation IDs | First transition to Ready verified; the next transition failed because the renderer emitted `status: ready`, which the parser rejected as an unsafe unquoted scalar. |
| Final status-only recovery | Board-scoped Trello update of only the run card's status metadata and list to Done, followed by response verification | Exit 0; card `6a6840c28e8cc4f0f1716831` is in configured Done list `6a16bbf1fea5389eb396372e` with status `done`. |

## Exit codes

- Workflow preflight: `0`.
- Sanitized typecheck and focused tests: `0`.
- Workflow-init dry-run: `0`.
- Live E2E: `1`.
- Recovery inspection: `0`.
- CLI transition recovery chain: `1` after the first verified transition.
- Final status-only recovery: `0`.

## Observed result

The live run stayed on the explicitly approved TestingBoard. It initialized the five missing canonical lists and preserved the existing Done list. It created and successfully closed its empty disposable list. The Work Unit card reached creation/checklist/update steps but cleanup failed for two product/harness reasons:

1. The live harness appended an `## Open Questions` section after creating an Inbox Work Unit. That section is valid only while status is Inbox, so cleanup could not parse and transition the card after the scenario had advanced.
2. After removing that run-only invalid section, transition to Ready succeeded, but rendering produced the unquoted line `status: ready`. The next parse rejected `ready` as an unsafe unquoted YAML scalar, so a multi-step transition could not continue.

Recovery remained board- and run-scoped. The single leaked run card was moved to Done without archival or deletion, and the disposable list was already closed empty. Canonical initialized status lists intentionally remain open. Credentials were not printed or persisted.

## Status

Failed.

## Remaining failures

- Prevent the live harness from appending status-invalid Open Questions content before multi-step cleanup.
- Make rendered Work Unit status values parseable across repeated transitions, including `ready`.
- Add offline regression coverage for create/render/parse and sequential transitions.
- Rerun sanitized gates and the authorized TestingBoard live E2E on a fresh run ID.
- Obtain an independent exact-snapshot review after the live command passes.
