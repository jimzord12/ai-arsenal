Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: verification
Revision: 5
Prerequisites: contract@3,plan@5,implementation@6
Status: superseded

# Commands

| Criterion | Command | Result |
| --- | --- | --- |
| Workflow preflight | `node.exe scripts/validate-monorepo-work-item.mjs --current --json` | Exit 0; valid verify route. |
| Authorized TestingBoard live E2E | `TRELLO_LIVE_E2E=1 TRELLO_LIVE_CREDENTIAL_SOURCE=process-env TRELLO_LIVE_BOARD_SELECTOR=TestingBoard TRELLO_LIVE_BOARD_ID=6a16bbf1fea5389eb39636b7 pnpm --filter @jz/ai-arsenal-trello-work-cli test:live` | Exit 1; 9 tests passed and the real production-path scenario failed in list update replay/collision verification. |
| Board-scoped recovery inspection | Exact run-marker card/list queries on TestingBoard | No new run card was created; the run-created disposable list was closed; no open leaked run list remained. |
| Production dry-run probe | Board-scoped `work lists update ... --dry-run` on canonical Review with a unique operation ID | Exit 0; returned `planned`, isolating the live failure to later update replay/collision behavior rather than dry-run itself. |
| Source/data-flow inspection | Inspect `updateBoardList` replay and postcondition logic | Found symbolic position mismatch: the recorded requested postcondition hashes `pos: "top"`, while Trello read-back normalizes position to a number. Exact update replay therefore becomes `partial` before semantic `positionMatches` can recover it. |

## Exit codes

- Workflow preflight: `0`.
- Live E2E: `1`.
- Recovery inspection: `0`.
- Isolated production dry-run: `0`.

## Observed result

The expanded live scenario reached real production list create and update boundaries. The disposable list was visibly renamed, but exact replay/collision verification failed. Data-flow inspection identified a real-only postcondition normalization defect: update replay compares a digest of Trello's numeric position with a digest recorded from symbolic `top`, so semantically correct position updates cannot recover exactly on real Trello.

Cleanup succeeded before the failed command returned. No new card was created, the disposable run list is closed, and no unrelated board was accessed.

## Status

Failed.

## Remaining failures

- Make update replay postcondition comparison semantic for symbolic `top`/`bottom` positions rather than comparing incompatible symbolic and normalized numeric digests.
- Add a RED regression where the write request uses `top` and read-back/action replay uses Trello's numeric position.
- Rerun sanitized gates and the fresh authorized TestingBoard live scenario.
- Complete independent exact-snapshot review after the live scenario passes.
