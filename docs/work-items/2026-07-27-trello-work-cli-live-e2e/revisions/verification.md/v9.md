Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: verification
Revision: 9
Prerequisites: contract@3,plan@5,implementation@10
Status: superseded

# Commands

| Criterion | Command / evidence | Result |
| --- | --- | --- |
| Exact staged snapshot | `git status --short`, `git diff --cached --check`, validator | 64 authorized staged paths; no unstaged/untracked files; cached check and workflow validation passed. |
| Authorized TestingBoard live E2E | Exact allowlisted command | Exit 0; 12/12 passed. |
| Live postcondition inspection | Board-scoped exact run-marker queries | Latest card in canonical Done; six temporary workflow lists, disposable list, and collision target all closed; prior recovery resources remain safe. |
| Independent exact-snapshot review | Read-only contract/plan/cached-source review | Failed with one High finding. |

## Observed result

Workflow-init parent recovery is premature after a partial multi-list initialization. The parent marker is attached to the first child list. On replay, finding that one matching parent marker causes immediate `recovered`, even if later child creation failed and one or more requested canonical lists remain missing. This can falsely certify an incomplete workflow and prevents safe resumption.

A matching parent marker must establish replay identity, not completion. Initialization must continue resolving all six requested postconditions, safely resume creation of still-missing run-owned canonical lists using deterministic child operation IDs, and return recovered only after every requested mapping is verified. If completion remains uncertain, it must return partial/ambiguous credential-free recovery evidence. A conflicting parent marker must continue to fail before mutation.

## Status

Failed.

## Remaining failures

- Separate parent identity match from complete-workflow postcondition verification.
- Add strict-TDD partial-creation regression: first child carries parent marker, later child is missing, exact replay resumes or reports partial rather than falsely recovering.
- Refresh the exact authorized staged snapshot and rerun gates, live E2E, cleanup inspection, and final independent review.
