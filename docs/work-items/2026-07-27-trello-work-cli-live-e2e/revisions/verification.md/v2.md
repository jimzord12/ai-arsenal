Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: verification
Revision: 2
Prerequisites: contract@3,plan@4,implementation@3
Status: superseded

# Commands

| Criterion | Command / inspection | Result |
| --- | --- | --- |
| Workflow preflight | `node.exe scripts/validate-monorepo-work-item.mjs --current --json` | Exit 0; valid route to `verify-monorepo-change`. |
| Snapshot path attribution | `git status --short` plus exact comparison of implementation@3 changed paths with plan@4 ordered-task and affected-path inventories | Failed before live mutation. `src/create.test.ts`, `src/update.test.ts`, `src/transition.test.ts`, and `src/reconcile.test.ts` changed for typed-fixture migration but are absent from plan@4's exact authorized paths. |
| Whitespace | `git diff --check` | Exit 0. |
| Artifact cleanup | shell existence check for `packages/trello-work-cli/*.tgz` | `tarballs=0`. |
| Live E2E | Not run | Deliberately withheld because the exact implementation snapshot exceeded the approved path inventory and was not eligible for live mutation verification. |

## Exit codes

- Workflow preflight: `0`.
- Whitespace: `0`.
- Tarball check: `0`.
- Live E2E: Not run.
- Path-attribution inspection: Not applicable.

## Observed result

Implementation@3 reports passing offline focused/package/root/workflow/validation gates and no Trello access. The worktree is not eligible to proceed to live verification because four changed typed-fixture test paths are outside plan@4's exact affected-path inventory. These migrations may be technically necessary, but verification cannot retroactively authorize them or edit the snapshot.

## Status

Failed.

## Remaining failures

- Obtain a fresh plan revision that explicitly authorizes `packages/trello-work-cli/src/create.test.ts`, `packages/trello-work-cli/src/update.test.ts`, `packages/trello-work-cli/src/transition.test.ts`, and `packages/trello-work-cli/src/reconcile.test.ts`, or remove those changes during implementation without weakening contract@3.
- Recreate the implementation handoff against the newly approved exact path inventory.
- Then run the confirmed `TestingBoard` live E2E and independent exact-snapshot review.
