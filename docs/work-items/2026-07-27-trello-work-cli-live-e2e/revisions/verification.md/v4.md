Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: verification
Revision: 4
Prerequisites: contract@3,plan@5,implementation@5
Status: superseded

# Commands

| Criterion | Command / inspection | Result |
| --- | --- | --- |
| Authorized TestingBoard live E2E | `TRELLO_LIVE_E2E=1 TRELLO_LIVE_CREDENTIAL_SOURCE=process-env TRELLO_LIVE_BOARD_SELECTOR=TestingBoard TRELLO_LIVE_BOARD_ID=6a16bbf1fea5389eb39636b7 pnpm --filter @jz/ai-arsenal-trello-work-cli test:live` | Exit 0; 7/7 tests passed. |
| Live postcondition inspection | Board-scoped queries for exact `[work-live-run:*]` markers | Both run cards verified in Done; both disposable run lists verified closed; no unrelated board accessed. |
| Package/root gates | Sanitized package format/lint/typecheck/test plus root format/lint/typecheck/test, workflow tests, living-workflow validation, and whitespace checks | Exit 0; package 197 passed with 1 ordinary-run live skip; root and 30 workflow tests passed. |
| Strict package validation | `pnpm --filter @jz/ai-arsenal-trello-work-cli validate`, immediate tarball removal, zero-tarball check | Exit 0; publint `All good!`; `tarballs=0`. |
| Independent exact-snapshot review | Fresh read-only review of contract@3, plan@5, implementation@5, source/tests, path inventory, and parent live evidence | Failed with two High findings. |

## Exit codes

- Live E2E: `0`.
- Postcondition inspection: `0`.
- Package/root/workflow gates: `0`.
- Strict package validation and cleanup: `0`.
- Independent review: Not applicable.

## Observed result

The real happy path and cleanup pass on the explicitly approved TestingBoard. However, independent inspection found that the separately invoked boundary matrix is not meaningful: its probes are hard-coded closures returning expected strings rather than exercising production board/list/transition/recovery behavior. The live happy-path body does not cover the contract-required unknown/ambiguous board, wrong-board list, occupied-list close, stale version, workflow-init/list mutation replay/collision, Work Unit replay, or injected recovery boundaries.

Workflow initialization also lacks durable top-level operation identity. It records child list-create operations but no parent workflow-init request fingerprint. Repeating the same operation ID after completion returns generic verified rather than recovered, and reusing that ID with changed intent can avoid collision detection when no lists are missing.

The reviewer also flagged broad `NEXT.md` content. This is not counted as an implementation failure: context@1 records that the broad user-authored baseline predated capture, while this implementation changed only the pipeline registration field. The stale baseline text will be reconciled only at the repository's reconciliation stage.

## Status

Failed.

## Remaining failures

- Replace hard-coded boundary-probe closures with real production calls or deterministic injected production seams that can fail when the corresponding behavior breaks; exercise real Trello where safe and deterministic injection only for outcomes that cannot safely be forced live.
- Add durable parent workflow-init operation fingerprint, exact replay recovery, changed-intent collision behavior, and focused tests.
- Rerun sanitized gates, a fresh authorized TestingBoard live E2E, and independent exact-snapshot review.
