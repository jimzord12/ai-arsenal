Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: verification
Revision: 6
Prerequisites: contract@3,plan@5,implementation@7
Status: superseded

# Commands

| Criterion | Command | Result |
| --- | --- | --- |
| Authorized TestingBoard live E2E | `TRELLO_LIVE_E2E=1 TRELLO_LIVE_CREDENTIAL_SOURCE=process-env TRELLO_LIVE_BOARD_SELECTOR=TestingBoard TRELLO_LIVE_BOARD_ID=6a16bbf1fea5389eb39636b7 pnpm --filter @jz/ai-arsenal-trello-work-cli test:live` | Exit 1; 9 tests passed; real scenario reached run-card cleanup and failed status/list synchronization. |
| Board-scoped recovery inspection | Exact run-marker card/list queries | Found one run card in the run-owned disposable list and that list open. |
| Recovery | CLI transitions using a temporary per-command `TRELLO_LIST_INBOX_ID` equal to the run-owned disposable list, then board-scoped list close | Four supported transitions verified the card in Done; disposable list close verified. No card/checklist deletion or archival. |

## Observed result

The harness creates the run card with a temporary effective Inbox mapping pointed at the disposable run list (`cardConfig`), but both normal cleanup and `finally` cleanup call `moveRunCardToDone` with the canonical `config`. Production transition correctly rejects the resulting status/list mismatch. Cleanup must retain the effective per-card configuration used at creation.

Recovery succeeded: run card `6a684a76569b947f049b3336` is verified in Done and disposable list `6a684a6bf3aaba99512c9a0b` is verified closed. No unrelated board was accessed.

## Status

Failed.

## Remaining failures

- Preserve the effective run-card list mapping outside the try block and use it for normal and recovery transitions.
- Add an offline regression proving cleanup uses the same temporary Inbox mapping as creation.
- Rerun sanitized gates and a fresh authorized TestingBoard live scenario.
