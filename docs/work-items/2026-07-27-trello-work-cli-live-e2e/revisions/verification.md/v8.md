Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: verification
Revision: 8
Prerequisites: contract@3,plan@5,implementation@9
Status: superseded

# Commands

| Criterion | Command | Result |
| --- | --- | --- |
| Staged snapshot preflight | Stage authorized `transition.ts`, `git diff --cached --check`, validator | 62 staged paths; no unstaged diff; cached check and validator passed. |
| Authorized TestingBoard live E2E | Exact allowlisted live command | Exit 1; 10 tests passed; workflow-init live replay/collision expectation failed. |
| Recovery inspection | Board-scoped list query for run ID / `Changed Done` | Found one empty run-created list `live-1785220896598-5efefe63 Changed Done`. |
| Recovery | Board-scoped CLI list close with unique recovery operation | Exit 0; list `6a684f26b1cd961eb21e7d4d` verified closed. |

## Observed result

After the correct zero-write complete-workflow fix, canonical initialization on an already-complete board returns verified with no durable marker. Repeating it is another verified no-op, not recovered. The live harness still expected recovered and then reused the same operation ID with a changed Done name. With no prior parent marker, that changed workflow legitimately had a missing list and created `Changed Done`, after which the harness failed its stale expectation.

The live replay/collision boundary must use a separate run-scoped workflow whose six uniquely tagged canonical names are genuinely missing. The first invocation may create those run-owned lists, exact replay can recover from their parent markers, changed intent can collide, and cleanup can close the empty run-owned lists. Canonical initialization itself must remain a zero-write no-op when already complete.

The one run-created list was empty, uniquely tagged by run ID, and was verified closed. No card was created and no unrelated board was accessed.

## Status

Failed.

## Remaining failures

- Update live initialization preflight to accept verified mutation-free canonical no-op behavior.
- Exercise workflow-init replay/collision using six uniquely run-tagged, run-owned temporary workflow list names, then close those empty lists during recovery-aware cleanup.
- Add offline regression for this complete-board/live-boundary split.
- Refresh the authorized staged snapshot and rerun live/final review.
