Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: plan
Revision: 3
Prerequisites: contract@2
Status: superseded

# Preconditions

- `change-contract.md` revision 2 remains current and unchanged; superseded contract@1, plan@1, and revision-request@1 archives remain byte-preserved except `Status: superseded`.
- Root `AGENTS.md`, `NEXT.md`, canonical-plan sections 4.1, 7.6, and 8, the monorepo pipeline, implementation skill, and package documentation are read before implementation.
- Fresh digest-bound user approval of this exact revision is recorded before product/test edits or Trello access.
- Implementation uses an isolated branch/worktree from verified `master` and preserves the user-authored `NEXT.md` baseline outside pipeline registration fields.
- Before live access, the operator confirms the dedicated testing-board selector, six status-list IDs, process environment as credential source, complete transition graph, and reconciliation source. Credential values are never displayed or persisted.
- Normal tests remain network-free. Live execution requires `TRELLO_LIVE_E2E=1` and an exact allowlisted board ID after selector resolution.
- No release, publication, global installation, board mutation, permanent list deletion, card/checklist archival or deletion, or selected-board persistence is authorized. Temporary package packing is authorized only inside the existing strict `validate` command; remove its generated tarball immediately afterward and do not install or distribute it.

## Ordered tasks

### 1. Record focused RED coverage for stateless boards and guarded list CRUD

- Paths: `packages/trello-work-cli/src/board.test.ts`, `packages/trello-work-cli/src/list-management.test.ts`, `packages/trello-work-cli/src/trello-client.test.ts`, `packages/trello-work-cli/src/config.test.ts`, `packages/trello-work-cli/src/cli.test.ts`, `packages/trello-work-cli/src/read-commands.test.ts`
- Inputs: Contract acceptance criteria for board listing/resolution, exact list-name uniqueness across open and closed lists, list create/read/update/close, caller-intent-stable operation replay/collision, ownership and occupancy guards, explicit per-command selection, no persisted board state, and credential redaction; failed verification revision 1.
- Output: Failing offline tests at the pure/injected-client/CLI seams before production changes.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/board.test.ts src/list-management.test.ts src/trello-client.test.ts src/config.test.ts src/cli.test.ts src/read-commands.test.ts`
- Expected result: New contract tests fail because board discovery, `--board`, and list CRUD do not yet exist; existing unrelated assertions remain stable.
- Rollback: Not applicable.

### 2. Implement read-only board discovery and exact stateless resolution

- Paths: `packages/trello-work-cli/src/board.ts`, `packages/trello-work-cli/src/trello-client.ts`, `packages/trello-work-cli/src/trello-types.ts`, `packages/trello-work-cli/src/config.ts`, `packages/trello-work-cli/src/read-commands.ts`
- Inputs: Board-resolution RED tests and existing transport/config/read boundaries.
- Output: Readable-board enumeration; resolution by readable 24-character ID or one exact name; safe no-match/ambiguity errors; list-membership validation; no `TRELLO_BOARD_ID` selection fallback.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/board.test.ts src/trello-client.test.ts src/config.test.ts src/read-commands.test.ts`
- Expected result: Focused tests pass with no board mutation or persistence path and no credential-bearing output.
- Rollback: Not applicable.

### 3. Implement board-scoped list CRUD with durable mutation identity

- Paths: `packages/trello-work-cli/src/list-management.ts`, `packages/trello-work-cli/src/list-management.test.ts`, `packages/trello-work-cli/src/mutation.ts`, `packages/trello-work-cli/src/mutation.test.ts`, `packages/trello-work-cli/src/trello-client.ts`, `packages/trello-work-cli/src/trello-client.test.ts`, `packages/trello-work-cli/src/trello-types.ts`
- Inputs: List CRUD, operation replay/collision, ambiguous/partial recovery, board ownership, configured-status-list occupancy, and read-back acceptance criteria.
- Output: Board-scoped list/read operations; create with name/position after rejecting any exact open/closed name match; update name/position; close/archive as delete; pre-write ownership/occupancy guards; durable operation fingerprints derived from caller intent rather than mutable inferred fields; canonical postconditions; no blind retry; read-back verification and credential-free recovery records.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/list-management.test.ts src/mutation.test.ts src/trello-client.test.ts`
- Expected result: Every list mutation family passes verified, dry-run, exact replay, collision, wrong-board, occupied-list, transport ambiguity, and post-write verification-failure tests; create rejects duplicate exact names; update replay after unrelated omitted-field drift resolves the original operation and reports postcondition drift rather than collision; no board/card deletion method exists.
- Rollback: Not applicable.

### 4. Expose explicit board and list commands through the CLI

- Paths: `packages/trello-work-cli/src/cli.ts`, `packages/trello-work-cli/src/cli.test.ts`, `packages/trello-work-cli/src/command-catalog.ts`
- Inputs: Required `--board <id-or-exact-name>` behavior and list CRUD public-command acceptance criteria.
- Output: `work boards list`; required `--board` for board-dependent commands; `work lists list`, `work lists create`, `work lists update`, and `work lists close`; dry-run/operation-ID options on mutations; resolved board ID injected into existing operations without persistence.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/board.test.ts src/list-management.test.ts src/cli.test.ts src/read-commands.test.ts`
- Expected result: Parser, help/catalog, ID/name selection, list CRUD routing, safety options, ownership checks, and unchanged local commands pass at the real Bun/injected CLI seams.
- Rollback: Not applicable.

### 5. Add explicit live E2E with card-status and disposable-list cleanup

- Paths: `packages/trello-work-cli/test/live-trello.e2e.test.ts`, `packages/trello-work-cli/test/live-support.ts`, `packages/trello-work-cli/package.json`
- Inputs: Confirmed live credentials/configuration; contract live-isolation, Work Unit, list CRUD, recovery, and cleanup criteria; failed verification revision 1.
- Output: Separately invoked bounded suite that preflights exact board/lists, creates a unique run ID, mutates only run-tagged cards/lists, covers approved happy paths plus unknown and ambiguous board names, wrong-board list, occupied configured-list close, stale version, dry-run/replay/collision for each list mutation family, Work Unit replay, and deterministically injectable partial/ambiguous recovery; moves all run cards to Done in `finally`, verifies disposable lists empty, closes only empty run-created lists, and emits credential-free recovery records for unresolved resources.
- Test command: `TRELLO_LIVE_E2E=1 pnpm --filter @jz/ai-arsenal-trello-work-cli test:live`
- Expected result: Complete confirmed configuration exits 0 with run-created cards in Done and empty disposable lists closed; missing opt-in/config exits before access; unresolved resources make the run fail visibly without force-close, archive/delete of cards, or secret leakage.
- Rollback: Reinvoke only the harness recovery mode with the preserved run ID and approved board selector: transition unresolved run cards to Done, verify disposable run lists empty, then close those lists. Stop and report if ownership or state is ambiguous.

### 6. Update version-matched documentation

- Paths: `packages/trello-work-cli/README.md`, `packages/trello-work-cli/assets/work-guide.md`, `packages/trello-work-cli/src/docs.ts`, `packages/trello-work-cli/src/docs.test.ts`, `packages/trello-work-cli/src/checklist.test.ts`, `packages/trello-work-cli/src/command-catalog.ts`
- Inputs: Revised public board/list contract, safety options, preflight, cleanup, and recovery criteria.
- Output: Help/docs for stateless `--board`, exact-name ambiguity, board listing, list CRUD terminology and commands, Trello close/archive semantics, occupied-status-list protection, explicit live invocation, card-status cleanup, empty-list close, and recovery evidence.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/docs.test.ts src/cli.test.ts`
- Expected result: Documentation/catalog tests pass; examples contain no credentials, persistent board state, board mutation, permanent list deletion, or card archival/deletion.
- Rollback: Not applicable.

### 7. Run final gates and freeze independent review evidence

- Paths: `packages/trello-work-cli/src/board.ts`, `packages/trello-work-cli/src/board.test.ts`, `packages/trello-work-cli/src/list-management.ts`, `packages/trello-work-cli/src/list-management.test.ts`, `packages/trello-work-cli/src/trello-client.ts`, `packages/trello-work-cli/src/trello-client.test.ts`, `packages/trello-work-cli/src/trello-types.ts`, `packages/trello-work-cli/src/config.ts`, `packages/trello-work-cli/src/config.test.ts`, `packages/trello-work-cli/src/read-commands.ts`, `packages/trello-work-cli/src/read-commands.test.ts`, `packages/trello-work-cli/src/mutation.ts`, `packages/trello-work-cli/src/mutation.test.ts`, `packages/trello-work-cli/src/cli.ts`, `packages/trello-work-cli/src/cli.test.ts`, `packages/trello-work-cli/src/checklist.test.ts`, `packages/trello-work-cli/src/command-catalog.ts`, `packages/trello-work-cli/src/docs.ts`, `packages/trello-work-cli/src/docs.test.ts`, `packages/trello-work-cli/test/live-trello.e2e.test.ts`, `packages/trello-work-cli/test/live-support.ts`, `packages/trello-work-cli/package.json`, `packages/trello-work-cli/README.md`, `packages/trello-work-cli/assets/work-guide.md`, `docs/work-items/2026-07-27-trello-work-cli-live-e2e/implementation-report.md`
- Inputs: Stable implementation bytes, all acceptance criteria, live results, and secret-free recovery evidence.
- Output: Passing package/root gates, implementation report, explicit task-path staged snapshot, and one independent review verdict on that exact snapshot.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli format && pnpm --filter @jz/ai-arsenal-trello-work-cli lint && pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck && pnpm --filter @jz/ai-arsenal-trello-work-cli test && pnpm --filter @jz/ai-arsenal-trello-work-cli validate && pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm test:workflow && node.exe scripts/validate-living-workflow.mjs && git diff --check && git diff --cached --check`
- Expected result: Every command exits 0 on the stable final snapshot; normal tests stay offline; reviewer finds no Critical/High board-isolation, list-CRUD, operation-identity, recovery, cleanup, or credential defect.
- Rollback: Not applicable.

## Affected paths

### Create

- `packages/trello-work-cli/src/board.ts`
- `packages/trello-work-cli/src/board.test.ts`
- `packages/trello-work-cli/src/list-management.ts`
- `packages/trello-work-cli/src/list-management.test.ts`
- `packages/trello-work-cli/test/live-trello.e2e.test.ts`
- `packages/trello-work-cli/test/live-support.ts`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/implementation-report.md`

### Modify

- `packages/trello-work-cli/src/trello-client.ts`
- `packages/trello-work-cli/src/trello-client.test.ts`
- `packages/trello-work-cli/src/trello-types.ts`
- `packages/trello-work-cli/src/config.ts`
- `packages/trello-work-cli/src/config.test.ts`
- `packages/trello-work-cli/src/read-commands.ts`
- `packages/trello-work-cli/src/read-commands.test.ts`
- `packages/trello-work-cli/src/mutation.ts`
- `packages/trello-work-cli/src/mutation.test.ts`
- `packages/trello-work-cli/src/cli.ts`
- `packages/trello-work-cli/src/cli.test.ts`
- `packages/trello-work-cli/src/checklist.test.ts`
- `packages/trello-work-cli/src/command-catalog.ts`
- `packages/trello-work-cli/src/docs.ts`
- `packages/trello-work-cli/src/docs.test.ts`
- `packages/trello-work-cli/package.json`
- `packages/trello-work-cli/README.md`
- `packages/trello-work-cli/assets/work-guide.md`

### Delete

- None.

## Verification commands

- `node.exe scripts/validate-monorepo-work-item.mjs --work-item 2026-07-27-trello-work-cli-live-e2e --json` — exits 0 at every expected route.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/board.test.ts src/list-management.test.ts src/trello-client.test.ts src/config.test.ts src/cli.test.ts src/read-commands.test.ts src/mutation.test.ts src/docs.test.ts` — focused offline suites pass after preserved RED evidence.
- `TRELLO_LIVE_E2E=1 pnpm --filter @jz/ai-arsenal-trello-work-cli test:live` — confirmed live environment exits 0 with cards in Done, empty run-created lists closed, and no secret leakage.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli format` — exits 0.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli lint` — exits 0.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck` — exits 0.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli test` — exits 0 without live access.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli validate` — exits 0 with the intentional packed boundary; the generated tarball is removed immediately and is neither installed nor distributed.
- `pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm test:workflow` — all exit 0 without live access.
- `node.exe scripts/validate-living-workflow.mjs` — exits 0.
- `git diff --check && git diff --cached --check` — exits 0.

## Rollback

1. Restore only task-owned repository paths from the isolated pre-implementation base before any authorized commit.
2. For live resources, use the preserved run ID and same approved board selector to move only unresolved run cards to Done.
3. Verify a run-created disposable list has no cards before closing it; never close a nonempty list or a list whose ownership is ambiguous.
4. Stop retries and retain credential-free recovery evidence for any unresolved card/list. Never mutate the board itself, permanently delete a list, or archive/delete a card/checklist.
