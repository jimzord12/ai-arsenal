Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: plan
Revision: 1
Prerequisites: contract@1
Status: superseded

# Preconditions

- `change-contract.md` revision 1 remains current and unchanged.
- Root `AGENTS.md`, `NEXT.md`, `docs/planning/CANONICAL_IMPLEMENTATION_PLAN.md` sections 4.1, 7.6, and 8, `docs/workflow/MONOREPO_WORK_ITEM_PIPELINE.md`, `.agents/skills/implement-monorepo-change/SKILL.md`, and applicable package documentation are read before implementation.
- Digest-bound explicit user approval is recorded before any product/test edit or Trello access.
- Implementation uses an isolated branch/worktree from the verified `master` commit and preserves the pre-existing user-authored `NEXT.md` baseline outside the pipeline registration fields.
- Before any live access, the operator supplies and confirms the dedicated testing board selector, the six test-list IDs, process environment as the credential source, the complete transition graph, and reconciliation source. Credential values are never displayed or persisted.
- Normal offline commands remain network-free. The live harness requires a separate `TRELLO_LIVE_E2E=1` opt-in and exact allowlisted board ID after selector resolution.
- No release, pack, publication, global installation, board/list mutation, archival, deletion, or persistent selected-board state is authorized.

## Ordered tasks

### 1. Add failing offline board-resolution and stateless CLI tests

- Paths: `packages/trello-work-cli/src/board.test.ts`, `packages/trello-work-cli/src/trello-client.test.ts`, `packages/trello-work-cli/src/config.test.ts`, `packages/trello-work-cli/src/cli.test.ts`, `packages/trello-work-cli/src/read-commands.test.ts`
- Inputs: Acceptance criteria for readable-board listing, ID/exact-name resolution, stable zero/ambiguous errors, explicit per-invocation selection, no persisted or ambient board state, board/list membership, and credential redaction; board-resolution and process test seams.
- Output: Focused RED tests proving the current CLI lacks the approved stateless board contract and fails safely without network access.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/board.test.ts src/trello-client.test.ts src/config.test.ts src/cli.test.ts src/read-commands.test.ts`
- Expected result: The new contract tests fail for missing board listing/resolution, `--board` routing, and membership validation while existing unrelated assertions remain stable.
- Rollback: Not applicable.

### 2. Implement read-only board discovery and exact stateless resolution

- Paths: `packages/trello-work-cli/src/board.ts`, `packages/trello-work-cli/src/trello-client.ts`, `packages/trello-work-cli/src/trello-types.ts`, `packages/trello-work-cli/src/config.ts`, `packages/trello-work-cli/src/read-commands.ts`
- Inputs: Board resolution acceptance criteria and test seams from Task 1; existing Trello transport, `WorkConfig`, and read-command boundaries.
- Output: Readable-board enumeration; resolution by readable 24-character board ID or one exact board name; stable no-match/ambiguity errors; safe board identity output; list-membership validation; removal of `TRELLO_BOARD_ID` as an ambient board-selection source.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/board.test.ts src/trello-client.test.ts src/config.test.ts src/read-commands.test.ts`
- Expected result: Focused board/config/read tests pass; no mutation method for boards/lists or persistence path exists; errors and outputs contain no credentials.
- Rollback: Not applicable.

### 3. Route every remote command through explicit `--board`

- Paths: `packages/trello-work-cli/src/cli.ts`, `packages/trello-work-cli/src/command-catalog.ts`, `packages/trello-work-cli/src/cli.test.ts`
- Inputs: Acceptance criteria requiring a selector on every board-dependent invocation; existing parser, command catalog, injected-client, and real Bun process seams.
- Output: A read-only `work boards list` command; `--board <id-or-exact-name>` on every board-dependent remote command; one resolved board ID injected into the existing operation boundary; no state write or environment fallback.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/board.test.ts src/cli.test.ts src/read-commands.test.ts`
- Expected result: Focused tests pass for ID/name selection, required selectors, ambiguity/no-match errors, command help/catalog parity, list ownership validation before mutation, and unchanged local/offline commands.
- Rollback: Not applicable.

### 4. Add the explicit live-E2E harness with status-only cleanup

- Paths: `packages/trello-work-cli/test/live-trello.e2e.test.ts`, `packages/trello-work-cli/test/live-support.ts`, `packages/trello-work-cli/package.json`
- Inputs: Live-harness acceptance criteria; confirmed process-environment credential source, allowlisted board ID, six list IDs, transition graph, and reconciliation source; real Bun process and operation/recovery seams.
- Output: A separately invoked, bounded live suite that preflights the exact board/lists, generates a unique run ID, creates and identifies only run-tagged cards, exercises the approved happy paths and essential realistic boundaries, transitions every disposable card to Done in `finally`, verifies final location, and emits credential-free recovery records for unresolved resources.
- Test command: `TRELLO_LIVE_E2E=1 pnpm --filter @jz/ai-arsenal-trello-work-cli test:live`
- Expected result: With complete confirmed live configuration, the suite exits 0 and every created card is verified in Done; without opt-in or complete allowlist/configuration it exits before Trello access with a stable preflight result; any unresolved card makes the run nonzero with recovery evidence and no hidden leak.
- Rollback: For test-created Trello state, rerun only the harness recovery/cleanup mode using the preserved run ID and the same approved board selector to transition unresolved run-owned cards to Done. Never archive or delete resources.

### 5. Document the stateless board and live-test contract

- Paths: `packages/trello-work-cli/README.md`, `packages/trello-work-cli/assets/work-guide.md`, `packages/trello-work-cli/src/docs.test.ts`, `packages/trello-work-cli/src/command-catalog.ts`
- Inputs: Public-behavior, safety, preflight, cleanup, and credential acceptance criteria; existing offline documentation contract.
- Output: Version-matched help and documentation for `boards list`, required `--board`, ID/exact-name semantics, ambiguity behavior, no persisted state, explicit live invocation, allowlisting, status-only cleanup, and recovery reporting.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/docs.test.ts src/cli.test.ts`
- Expected result: Documentation/catalog tests pass and no example contains credential values, board mutation, archival/deletion, or persisted selection.
- Rollback: Not applicable.

### 6. Run final gates and freeze an independent-review snapshot

- Paths: `packages/trello-work-cli/src/board.ts`, `packages/trello-work-cli/src/board.test.ts`, `packages/trello-work-cli/src/trello-client.ts`, `packages/trello-work-cli/src/trello-client.test.ts`, `packages/trello-work-cli/src/trello-types.ts`, `packages/trello-work-cli/src/config.ts`, `packages/trello-work-cli/src/config.test.ts`, `packages/trello-work-cli/src/read-commands.ts`, `packages/trello-work-cli/src/read-commands.test.ts`, `packages/trello-work-cli/src/cli.ts`, `packages/trello-work-cli/src/cli.test.ts`, `packages/trello-work-cli/src/command-catalog.ts`, `packages/trello-work-cli/src/docs.test.ts`, `packages/trello-work-cli/test/live-trello.e2e.test.ts`, `packages/trello-work-cli/test/live-support.ts`, `packages/trello-work-cli/package.json`, `packages/trello-work-cli/README.md`, `packages/trello-work-cli/assets/work-guide.md`, `docs/work-items/2026-07-27-trello-work-cli-live-e2e/implementation-report.md`
- Inputs: All acceptance criteria and test seams; stable implementation bytes; live result and recovery evidence with secrets excluded.
- Output: Passing package/root gates, implementation report, explicit task-path staged snapshot, and one independent review verdict over that exact snapshot.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli format && pnpm --filter @jz/ai-arsenal-trello-work-cli lint && pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck && pnpm --filter @jz/ai-arsenal-trello-work-cli test && pnpm --filter @jz/ai-arsenal-trello-work-cli validate && pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm test:workflow && node.exe scripts/validate-living-workflow.mjs && git diff --check && git diff --cached --check`
- Expected result: Every command exits 0 on the stable final snapshot; normal tests remain offline; the independent reviewer reports no Critical or High correctness, isolation, mutation-boundary, recovery, or credential finding.
- Rollback: Not applicable.

## Affected paths

### Create

- `packages/trello-work-cli/src/board.ts`
- `packages/trello-work-cli/src/board.test.ts`
- `packages/trello-work-cli/test/live-trello.e2e.test.ts`
- `packages/trello-work-cli/test/live-support.ts`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/implementation-report.md`

### Modify

- `packages/trello-work-cli/src/trello-client.ts`
- `packages/trello-work-cli/src/trello-client.test.ts`
- `packages/trello-work-cli/src/trello-types.ts`
- `packages/trello-work-cli/src/config.ts`
- `packages/trello-work-cli/src/config.test.ts`
- `packages/trello-work-cli/src/read-commands.ts`
- `packages/trello-work-cli/src/read-commands.test.ts`
- `packages/trello-work-cli/src/cli.ts`
- `packages/trello-work-cli/src/cli.test.ts`
- `packages/trello-work-cli/src/command-catalog.ts`
- `packages/trello-work-cli/src/docs.test.ts`
- `packages/trello-work-cli/package.json`
- `packages/trello-work-cli/README.md`
- `packages/trello-work-cli/assets/work-guide.md`

### Delete

- None.

## Verification commands

- `node.exe scripts/validate-monorepo-work-item.mjs --work-item 2026-07-27-trello-work-cli-live-e2e --json` — exits 0 and routes to the stage expected at each handoff.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --coverage=false --runTestsByPath src/board.test.ts src/trello-client.test.ts src/config.test.ts src/cli.test.ts src/read-commands.test.ts src/docs.test.ts` — focused offline suites pass after recorded RED evidence.
- `TRELLO_LIVE_E2E=1 pnpm --filter @jz/ai-arsenal-trello-work-cli test:live` — on explicitly confirmed environment and board/list allowlist, exits 0 with every run-created card verified in Done and no credential leakage.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli format` — exits 0.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli lint` — exits 0.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck` — exits 0.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli test` — exits 0 without live Trello access.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli validate` — exits 0 with the intentional packed boundary.
- `pnpm format:check` — exits 0.
- `pnpm lint` — exits 0.
- `pnpm typecheck` — exits 0.
- `pnpm test` — exits 0 without live Trello access.
- `pnpm test:workflow` — exits 0.
- `node.exe scripts/validate-living-workflow.mjs` — exits 0.
- `git diff --check && git diff --cached --check` — exits 0.

## Rollback

1. For repository changes, restore only the task-owned paths from the isolated pre-implementation base before any authorized commit.
2. For live test resources, use the preserved run ID, same approved board selector, and configured transitions to move only unresolved run-owned cards to Done and verify them there.
3. If a card cannot be transitioned or verified, stop retries, retain the credential-free recovery record, and report the leak explicitly. Never archive/delete it or touch pre-existing resources.
