Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: implementation
Revision: 5
Prerequisites: contract@3,plan@5,approval@4
Status: superseded

# Changed paths

- `packages/trello-work-cli/test/live-support.ts`
- `packages/trello-work-cli/test/live-trello.e2e.test.ts`
- `packages/trello-work-cli/src/transition.ts`
- `packages/trello-work-cli/src/transition.test.ts`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/implementation-report.md`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/revisions/implementation-report.md/v4.md`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/revisions/verification.md/v3.md`
- `NEXT.md` (pipeline-step value only)

# Root cause and decisions

- The live fixture retained the draft-only `Open Questions` section. Once a status transition changed the persisted Work Unit from Inbox, the document correctly became invalid and later cleanup could not parse it.
- Transition rendering reused the general renderer's plain status scalar. The parser rejects `ready` at this repeated-transition boundary, so a first transition could persist a description that the next transition could not consume.
- Live draft preparation now removes the Inbox-only section before card creation while preserving run markers.
- Transition descriptions now serialize the target status as a JSON-quoted YAML scalar before local parse validation and remote mutation. This keeps the repair within the authorized transition boundary and leaves the unlisted Work Unit schema implementation untouched.

# Strict TDD evidence

- RED: the focused live-support regression failed because `prepareLiveWorkUnitDraft` did not exist.
- RED: the sequential `inbox -> ready -> in_progress` transition regression rejected the second operation with `metadata line 5: unsafe or unquoted scalar`.
- GREEN: after the two minimal repairs, the focused command passed 2 suites and 13 tests, with the explicit real-live scenario skipped.

# Offline verification

- Package format check: exit 0.
- Package lint: exit 0.
- Package typecheck: exit 0.
- Sanitized package test: exit 0; 18 suites passed, 197 tests passed, and 1 explicit real-live scenario skipped.
- No Trello access, live claim, reconciliation, commit, push, publish, install, or release occurred.

# Supersession and handoff

- Failed verification@3 is archived as `revisions/verification.md/v3.md` with only its status changed to `superseded`.
- Implementation@4 is archived as `revisions/implementation-report.md/v4.md` with only its status changed to `superseded`.
- This implementation@5 is ready for independent verification, including any separately authorized fresh live run.
