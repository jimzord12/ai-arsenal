Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: implementation
Revision: 8
Prerequisites: contract@3,plan@5,approval@4
Status: superseded

# Repair outcome

- Preserved one effective per-card `WorkConfig` for the run card from creation through both normal and `finally` cleanup.
- The effective configuration maps Inbox to the run-owned disposable list while retaining the canonical Done mapping.
- Normal cleanup and recovery cleanup now pass that effective configuration into the production transition path; final verification still compares the read-back list with canonical `config.listIds.done`.
- Recovery evidence and leak reporting remain unchanged.

# Changed paths in this repair

- `packages/trello-work-cli/test/live-support.ts`
- `packages/trello-work-cli/test/live-trello.e2e.test.ts`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/implementation-report.md`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/revisions/implementation-report.md/v7.md`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/revisions/verification.md/v6.md`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/verification.md` (failed current artifact archived, then removed for fresh verification)
- `NEXT.md` (pipeline-step value only)

# Root cause

The run card was created in the disposable list with a local `cardConfig` whose Inbox mapping pointed to that list. The variable was scoped inside the scenario body, while normal cleanup and `finally` cleanup passed canonical `config` to `moveRunCardToDone`. Production transition synchronization correctly rejected an Inbox document whose actual list did not equal canonical Inbox.

# Strict TDD evidence

- RED: added `cleans a run card through production transitions with its temporary Inbox mapping`. After correcting test-fixture identifiers, the focused suite failed as expected because `createRunCardConfig` was absent; 9 tests passed, 1 failed, and the explicit live test remained skipped.
- GREEN: extracted `createRunCardConfig`, exposed the production cleanup helper through the injected `TransitionClient` seam, hoisted `effectiveCardConfig`, and used it for creation, replay, normal cleanup, and `finally` cleanup.
- The regression executes real `transitionWorkUnit` behavior over sequential transitions from a card physically in the disposable Inbox through canonical Done. It would fail with `RECONCILIATION_REQUIRED` if cleanup lost the temporary mapping.

# Offline verification

- Focused regression: exit 0; 1 selected test passed.
- Package format initially identified the two edited files; Prettier was run only on those authorized paths, then package format passed.
- Focused sanitized live-support suite: exit 0; 10 tests passed and 1 explicit live scenario skipped.
- Package lint: exit 0.
- Package typecheck: exit 0.
- Sanitized package test: exit 0; 18 suites passed, 203 tests passed, and 1 explicit live scenario skipped.
- Archive copies were made before current-artifact replacement/removal; the targeted archive diffs changed only each `Status` line.
- No Trello access, live reconciliation, commit, push, installation, packing, publication, or release occurred.

# Supersession and handoff

- Implementation@7 is archived at `revisions/implementation-report.md/v7.md` with only `Status` changed to `superseded`.
- Failed verification@6 is archived at `revisions/verification.md/v6.md` with only `Status` changed to `superseded`.
- Fresh independent verification must run the separately authorized live scenario and exact-snapshot review. This implementation stage makes no live-success claim.

Deviations: None.
