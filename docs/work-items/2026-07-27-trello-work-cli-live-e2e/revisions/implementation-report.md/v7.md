Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: implementation
Revision: 7
Prerequisites: contract@3,plan@5,approval@4
Status: superseded

# Repair outcome

- Fixed real Trello exact update replay when caller intent records symbolic `pos: "top"` but Trello read-back and action replay expose a numeric position.
- Replay now applies the recorded postcondition digest only when the requested position is already canonical numeric data. Symbolic positions proceed to the existing semantic `verify`/`positionMatches` path.
- Name, closed-state, board, semantic-position, omitted-field postcondition drift, and changed-request operation-ID collision checks remain enforced.

# Changed paths in this repair

- `packages/trello-work-cli/src/list-management.ts`
- `packages/trello-work-cli/src/list-management.test.ts`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/implementation-report.md`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/revisions/implementation-report.md/v6.md`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/revisions/verification.md/v5.md`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/verification.md` (failed current artifact archived, then removed for fresh verification)
- `NEXT.md` (pipeline-step value only)

# Root cause

`marker()` records the requested update postcondition, including symbolic `top`. During replay, `replay()` hashed Trello's numeric read-back and rejected it against that symbolic hash before `verify()` could evaluate whether the numeric position was semantically topmost. The digest is representation-safe for numeric positions, but not for Trello-normalized symbolic positions.

# Strict TDD evidence

- RED: added `recovers an exact symbolic top update replay after numeric Trello read-back` using a structured fake that converts the symbolic write into a numeric stored/read-back position. The focused test failed with expected `partial` instead of `recovered` on the exact same-ID replay.
- GREEN: restricted the early digest comparison to numeric requested positions. The focused list-management suite passed 22/22, including changed-request collision, omitted-position drift, read-back drift, and exact numeric replay coverage.
- GREEN: the sanitized package suite passed 18/18 suites, 202 tests passed, and the one explicit live scenario remained skipped.

# Offline verification

- Package format: exit 0.
- Package lint: exit 0.
- Package typecheck: exit 0.
- Focused sanitized list-management suite: exit 0; 22 tests passed.
- Sanitized package test: exit 0; 18 suites passed, 202 tests passed, 1 explicit live scenario skipped.
- Archive byte checks confirmed implementation@6 and failed verification@5 differ from their current artifacts only by `Status: superseded`.
- No Trello access, live reconciliation, commit, push, installation, packing, publication, or release occurred.

# Supersession and handoff

- Implementation@6 is archived at `revisions/implementation-report.md/v6.md` with only `Status` changed to `superseded`.
- Failed verification@5 is archived at `revisions/verification.md/v5.md` with only `Status` changed to `superseded`.
- Fresh independent verification must run the separately authorized live scenario and exact-snapshot review. This implementation stage makes no live-success claim.

Deviations: None.
