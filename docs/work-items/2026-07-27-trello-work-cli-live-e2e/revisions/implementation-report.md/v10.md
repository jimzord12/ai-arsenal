Work item: 2026-07-27-trello-work-cli-live-e2e
Artifact: implementation
Revision: 10
Prerequisites: contract@3,plan@5,approval@4
Status: superseded

# Repair outcome

- Preserved canonical workflow initialization as a verified zero-write no-op when the allowlisted board already contains the complete canonical workflow.
- Added a separate live replay/collision fixture whose six status-list names are uniquely prefixed with the current run marker and whose `listIds` mapping is empty.
- The first run-workflow initialization creates exactly those six missing run-owned lists; exact same-operation replay recovers from the parent marker attached to child-create history; changed intent with the same operation ID returns partial collision evidence without creating `Changed Done`.
- All six temporary workflow list IDs are explicitly recorded as run-owned. Recovery-aware cleanup also recognizes their marker names, verifies each list empty, closes only open empty run-owned lists, and reads back closed state.
- No production list-management behavior changed.

# Strict TDD evidence

- RED: added the offline complete-board/live-boundary regression before the fixture helper existed. The focused live-support suite failed with `createRunWorkflowFixture is not a function`; 10 tests passed, 1 failed, and the explicit live scenario remained skipped.
- GREEN: implemented the run-scoped six-list fixture and routed the live scenario through it while leaving canonical initialization unchanged.
- Focused sanitized suite: exit 0; 11 tests passed and 1 explicit live scenario skipped.

# Changed paths in this repair

- `packages/trello-work-cli/test/live-support.ts`
- `packages/trello-work-cli/test/live-trello.e2e.test.ts`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/implementation-report.md`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/revisions/implementation-report.md/v9.md`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/revisions/verification.md/v8.md`
- `docs/work-items/2026-07-27-trello-work-cli-live-e2e/verification.md` (failed current artifact archived, then removed for fresh verification)
- `NEXT.md` (pipeline-step value only)

# Sanitized verification

- Package format initially reported only the two edited test paths; Prettier was applied to those authorized files and package format then exited 0.
- Package lint and typecheck: exit 0.
- Package offline test: exit 0; 18 suites passed, 205 tests passed, and 1 explicit live scenario skipped.
- Strict package validation: exit 0; publint reported `All good!`; no tarball remained.
- All test commands removed inherited live Trello variables. No live Trello access or reconciliation occurred.

# Supersession and handoff

- Implementation@9 is archived at `revisions/implementation-report.md/v9.md` with only `Status` changed to `superseded`.
- Failed verification@8 is archived at `revisions/verification.md/v8.md` with only `Status` changed to `superseded`.
- The plan-authorized task paths, complete active work-item directory, and `NEXT.md` registration are staged as the exact implementation@10 verification candidate. Unrelated paths are excluded.
- Fresh verification must review the exact cached snapshot, run the authorized live scenario, inspect run-scoped cleanup, and independently verify the remaining contract criteria. This implementation stage makes no fresh live-success claim.
- No commit, push, install, publication, release, board mutation, card/list reconciliation, or live access occurred.

Deviations: None.
