Work item: 2026-07-26-trello-work-unit-cli
Artifact: plan
Revision: 1
Prerequisites: contract@1
Status: superseded

# Preconditions

- `change-contract.md` revision 1 remains ready and unchanged.
- Root `AGENTS.md`, the monorepo pipeline, and the implementation-stage skill govern execution.
- The user explicitly approves this exact plan before `approval.md` is created or product/test files change.
- Implementation uses a dedicated branch and isolated worktree from current `master`; planning artifacts are transferred only through the repository workflow's approved implementation mechanism.
- Trello board/list configuration and credentials remain unavailable; every test uses local fixtures or injected fakes and no remote call is permitted.
- Engineering depth remains production-pragmatic and the review budget is one independent review plus at most one focused blocking correction cycle.

## Ordered tasks

### 1. Establish the package boundary and first failing CLI smoke tests

- Paths: `packages/trello-work-cli/package.json`, `packages/trello-work-cli/tsconfig.json`, `packages/trello-work-cli/jest.config.cjs`, `packages/trello-work-cli/jest-transformer.cjs`, `packages/trello-work-cli/src/cli.test.ts`, `pnpm-lock.yaml`
- Inputs: Acceptance criteria for workspace participation, real executable behavior, stable JSON output, and no runtime dependency; repository package conventions.
- Output: A private `@jz/ai-arsenal-trello-work-cli` package exposing `work`, with focused tests that fail because CLI production behavior does not yet exist.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/cli.test.ts`
- Expected result: RED—Jest discovers the focused CLI tests and fails for the missing executable/CLI implementation rather than configuration errors.
- Rollback: Not applicable.

### 2. Implement canonical Work Unit parsing and rendering by vertical TDD cycles

- Paths: `packages/trello-work-cli/src/work-unit.test.ts`, `packages/trello-work-cli/src/work-unit.ts`, `packages/trello-work-cli/assets/work-unit-metadata.schema.json`, `packages/trello-work-cli/test/fixtures/valid-draft.md`
- Inputs: Canonical fenced-YAML contract, metadata/section validation acceptance criteria, deterministic rendering seam, and strict 24-hex Trello ID decision.
- Output: Typed safe parser, validator, and deterministic renderer with the schema stored as the package's canonical machine-readable contract.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/work-unit.test.ts`
- Expected result: Each focused case is observed RED before its smallest implementation, then the full Work Unit suite passes for valid drafts and realistic malformed/unsafe/authority-invalid documents.
- Rollback: Not applicable.

### 3. Implement authority-aware creation planning and the `idShort` allocation design

- Paths: `packages/trello-work-cli/src/create-plan.test.ts`, `packages/trello-work-cli/src/create-plan.ts`
- Inputs: Acceptance criteria for read-only dry-run planning, system-managed fields, intended Inbox transaction, `id = "WU-" + idShort`, unique operation identity, and ambiguous-failure recovery.
- Output: A deterministic typed dry-run plan that names unresolved configuration, intended create/update/read-back verification operations, immutable/system-managed fields, and the server-authoritative ID strategy without performing I/O.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/create-plan.test.ts`
- Expected result: RED is observed before implementation; then valid drafts produce stable plans and invalid authority/state inputs fail closed.
- Rollback: Not applicable.

### 4. Complete the real `work` command boundary

- Paths: `packages/trello-work-cli/src/cli.test.ts`, `packages/trello-work-cli/src/cli.ts`, `packages/trello-work-cli/src/bin.ts`, `packages/trello-work-cli/test/fixtures/invalid-draft.md`
- Inputs: CLI acceptance criteria, parser and plan modules, stdout/stderr separation, stable codes, dry-run-only safety boundary, and process test seam.
- Output: `work create --dry-run --file <path> --output json`, help, invalid-document errors, argument errors, and a fail-before-mutation non-dry-run response.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/cli.test.ts`
- Expected result: Focused RED/GREEN cycles complete; real Bun process tests prove JSON stdout, diagnostics/errors, exit statuses, and no remote effects.
- Rollback: Not applicable.

### 5. Document and verify the package

- Paths: `packages/trello-work-cli/README.md`, `packages/trello-work-cli/package.json`
- Inputs: Skill/CLI responsibility boundary, canonical contract, offline command behavior, deferred configuration, `idShort` rationale, and partial-failure rule.
- Output: Proportional operating documentation and package scripts for format, lint, typecheck, test, pack, and strict validation.
- Test command: `pnpm --filter @jz/ai-arsenal-trello-work-cli format && pnpm --filter @jz/ai-arsenal-trello-work-cli lint && pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck && pnpm --filter @jz/ai-arsenal-trello-work-cli test && pnpm --filter @jz/ai-arsenal-trello-work-cli validate`
- Expected result: Every package-local gate exits 0; tests produce no remote mutations; strict package validation accepts the intended executable boundary.
- Rollback: Not applicable.

### 6. Freeze and independently review the implementation snapshot

- Paths: `packages/trello-work-cli/package.json`, `packages/trello-work-cli/tsconfig.json`, `packages/trello-work-cli/jest.config.cjs`, `packages/trello-work-cli/jest-transformer.cjs`, `packages/trello-work-cli/assets/work-unit-metadata.schema.json`, `packages/trello-work-cli/src/bin.ts`, `packages/trello-work-cli/src/cli.ts`, `packages/trello-work-cli/src/cli.test.ts`, `packages/trello-work-cli/src/create-plan.ts`, `packages/trello-work-cli/src/create-plan.test.ts`, `packages/trello-work-cli/src/work-unit.ts`, `packages/trello-work-cli/src/work-unit.test.ts`, `packages/trello-work-cli/test/fixtures/valid-draft.md`, `packages/trello-work-cli/test/fixtures/invalid-draft.md`, `packages/trello-work-cli/README.md`, `pnpm-lock.yaml`
- Inputs: Complete contract mapping, green focused gates, exact staged diff, and bounded review rules.
- Output: One independent correctness/security/contract review of the frozen snapshot; blocking findings, if any, receive one focused TDD correction and fresh review.
- Test command: `git diff --cached --check`
- Expected result: No whitespace errors and an independent PASS on the exact staged snapshot, or a bounded blocking finding routed back through focused RED/GREEN verification.
- Rollback: Not applicable.

### 7. Run aggregate repository verification

- Paths: `packages/trello-work-cli/**`, `pnpm-lock.yaml`
- Inputs: Independently accepted package snapshot and repository verification contract.
- Output: Grounded final evidence that the new package does not regress existing packages or workflow state.
- Test command: `pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm test:workflow && pnpm validate:workflow && git diff --cached --check`
- Expected result: Every command exits 0, including existing package tests and both workflow validators.
- Rollback: Not applicable.

## Affected paths

- Create: `packages/trello-work-cli/package.json`
- Create: `packages/trello-work-cli/tsconfig.json`
- Create: `packages/trello-work-cli/jest.config.cjs`
- Create: `packages/trello-work-cli/jest-transformer.cjs`
- Create: `packages/trello-work-cli/assets/work-unit-metadata.schema.json`
- Create: `packages/trello-work-cli/src/bin.ts`
- Create: `packages/trello-work-cli/src/cli.ts`
- Create: `packages/trello-work-cli/src/cli.test.ts`
- Create: `packages/trello-work-cli/src/create-plan.ts`
- Create: `packages/trello-work-cli/src/create-plan.test.ts`
- Create: `packages/trello-work-cli/src/work-unit.ts`
- Create: `packages/trello-work-cli/src/work-unit.test.ts`
- Create: `packages/trello-work-cli/test/fixtures/valid-draft.md`
- Create: `packages/trello-work-cli/test/fixtures/invalid-draft.md`
- Create: `packages/trello-work-cli/README.md`
- Modify: `pnpm-lock.yaml`

Implementation-stage and later pipeline evidence will additionally create their required files under `docs/work-items/2026-07-26-trello-work-unit-cli/` and reconciliation will update `NEXT.md` plus the canonical plan, as governed by those stages rather than by product implementation tasks.

## Verification commands

- `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/work-unit.test.ts` — focused parser/renderer suite passes after observed RED/GREEN cycles.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/create-plan.test.ts` — focused dry-run planner suite passes after observed RED/GREEN cycles.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/cli.test.ts` — real-process CLI suite passes after observed RED/GREEN cycles.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli format` — package formatting check exits 0.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli lint` — package lint exits 0.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck` — strict TypeScript exits 0.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli test` — complete package suite exits 0.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli validate` — strict package validation exits 0.
- `pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm test:workflow && pnpm validate:workflow` — aggregate repository gates exit 0.
- `git diff --cached --check` — frozen final snapshot has no whitespace errors.

## Rollback

Not applicable. This work item adds source, tests, documentation, and workspace lockfile metadata only; it performs no Trello mutation, release, installation, or other stateful external action.
