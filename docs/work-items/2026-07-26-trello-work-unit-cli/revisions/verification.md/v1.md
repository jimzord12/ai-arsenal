Work item: 2026-07-26-trello-work-unit-cli
Artifact: verification
Revision: 1
Prerequisites: contract@2,plan@2,implementation@1
Status: superseded

# Commands

| Source / criterion | Command or observation | Expected | Actual |
| --- | --- | --- | --- |
| Workflow preflight | `node scripts/validate-monorepo-work-item.mjs --current --json` and `--work-item 2026-07-26-trello-work-unit-cli --json` | Valid route to verification | Passed; both reported `valid: true` and `nextSkill: "verify-monorepo-change"`. |
| Changed snapshot | `git status --short --branch`; worktree/base inspection; 53-file source/evidence hash manifest | Dedicated branch at base `d351906`; intended uncommitted package/work-item changes preserved | Passed. Branch `feat/trello-work-unit-cli`; HEAD and merge-base `d351906`; final candidate digest excluding disposable `.turbo`/coverage output: `1d44433d019e06bdf686217400f868fa20fc15c075368b77ac46e9e3a6e42bbb`. |
| Canonical parser/render/authority | `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/work-unit.test.ts --coverage=false` | Focused suite passes | Passed: 1 suite, 25 tests. |
| Config/redaction/references/errors | `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/config.test.ts src/reference.test.ts src/errors.test.ts --coverage=false` | Focused suites pass | Passed: 3 suites, 23 tests. |
| Trello API v1 injected transport | `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/trello-client.test.ts --coverage=false` | Exact offline transport tests pass; no live Trello access | Passed: 1 suite, 6 tests. |
| Offline docs | `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/docs.test.ts --coverage=false` | Complete deterministic docs modes pass | Passed: 1 suite, 6 tests. |
| Read commands / real CLI | `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/read-commands.test.ts src/cli.test.ts --coverage=false` | Read-only commands, JSON/stderr/exits pass | Passed: 2 suites, 47 tests. |
| Mutation/version primitives | `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/mutation.test.ts src/version.test.ts --coverage=false` | Dry-run, version, operation-ID, verification/recovery tests pass | Passed: 2 suites, 8 tests. |
| Create/update | `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/create.test.ts src/update.test.ts --coverage=false` | Verified fake-transport mutations pass | Passed: 2 suites, 21 tests. |
| Transition/reconcile/checklist | `pnpm --filter @jz/ai-arsenal-trello-work-cli test -- --runTestsByPath src/transition.test.ts src/reconcile.test.ts src/checklist.test.ts --coverage=false` | Configured policy and checklist behavior pass | Passed: 3 suites, 22 tests. |
| Package format | `pnpm --filter @jz/ai-arsenal-trello-work-cli format` | Exit 0 after the plan's focused commands | **Failed initially (exit 1):** focused invocations inherited the package script's `--coverage`, generated `coverage/`, and Prettier reported 25 generated files. After removing only disposable coverage output, the same command passed; the initial required-command failure is preserved. |
| Package lint | `pnpm --filter @jz/ai-arsenal-trello-work-cli lint` | Exit 0 | Passed. |
| Package typecheck | `pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck` | Exit 0 | Passed. |
| Complete package tests | `pnpm --filter @jz/ai-arsenal-trello-work-cli test` | Exit 0 | Passed: 15 suites, 158 tests. |
| Package validation | `pnpm --filter @jz/ai-arsenal-trello-work-cli validate` | Strict publint and packed boundary pass | Passed; publint reported `All good!`. |
| Aggregate formatting | `pnpm format:check` | Exit 0 on cleaned disposable-output state | Passed. |
| Aggregate lint | `pnpm lint` | Exit 0 | Passed: 3 packages. |
| Aggregate typecheck | `pnpm typecheck` | Exit 0 | Passed: 3 packages. |
| Aggregate tests | `pnpm test` | Exit 0 | Passed: Trello CLI 15/158; features CLI 9/154; no live Trello access. |
| Workflow tests | `pnpm test:workflow` | Exit 0 | Passed: 30 tests. |
| Workflow validators | `pnpm validate:workflow`; `node scripts/validate-living-workflow.mjs` | Exit 0 | Passed; current work item remained valid at verification route before this record. |
| Whitespace | `git diff --check`; `git diff --cached --check` | Exit 0 | Passed. No files were staged. |
| Independent semantic review — replay/version ordering | Manual source/test review of recovery-aware handlers and CLI boundaries; independent coverage-free Jest run of 6 suites/77 tests | Exact operation replay is idempotently recoverable with zero additional writes | **Failed:** `update.ts:79-102`, `transition.ts:110-130`, `reconcile.ts:95-120`, and `checklist.ts:74-84`, with replay lookups at `167-181`, `261-275`, and `347-364`, enforce `--if-version` before persisted operation-record replay. A successful first mutation changes the remote version, so replaying the same operation ID and original version fails stale before the required exact recovery path. Existing green tests do not prove this combined replay-plus-version behavior for each production handler. The independent focused run itself passed 6 suites/77 tests and preserved its candidate/status digests. |
| Independent semantic review — update replay postcondition | Manual inspection of `update.ts:97-108` and `update.test.ts:214-235` | A matching operation marker returns `recovered` only when the complete requested postcondition still holds | **Failed:** metadata/description replay returns `recovered` from a matching historical marker without verifying the current title, metadata, sections, or rendered description. A later drifted card retaining the marker is falsely reported as recovered, and existing tests cover only an unchanged card and a changed request fingerprint. |
| Safety boundary | Manual observation of commands, status, and outputs | No credentials read; no network mutation, product edit, stage, commit, push, release, publish, pack-for-distribution, or global install | Passed. Deterministic tests used injected fakes; `publint --pack` was package validation only and produced no retained artifact. |

## Exit codes

- Both workflow preflight commands: `0`.
- Eight focused test commands: `0` each.
- Initial package format command: `1`.
- Package format rerun after deleting disposable coverage output: `0`.
- Package lint, typecheck, complete test, and validate: `0` each.
- Root format, lint, typecheck, test, workflow test, workflow validation, and living-workflow validation: `0` each.
- `git diff --check` and `git diff --cached --check`: `0` each.
- Manual observations: Not applicable.

## Observed result

The implementation is broadly executable and all deterministic package/root suites pass, including 158 Trello CLI tests, strict publint, aggregate repository tests, and workflow validation. Verification nevertheless fails on two independent semantic findings: exact operation replay is ordered after stale-version rejection in multiple production mutation families, and update replay trusts a historical marker without re-verifying the complete current postcondition. Both violate the contract's operation-ID idempotency/read-back recovery requirements. The exact plan command sequence also produced a reproducible package-format failure from generated coverage output; cleanup made formatting pass but does not erase the observed nonzero required command.

No real Trello credentials were loaded and no Trello request or mutation was performed. The worktree remains intentionally uncommitted and unstaged.

## Status

Failed.

## Remaining failures

- Move exact persisted operation-record replay/collision evaluation ahead of stale-version rejection for update, transition, reconcile, and checklist mutation handlers, while retaining stale-version rejection before every new write. Add production-handler tests for same operation ID + same request + original `--if-version` (recovered, zero writes) and same ID + changed request (fail closed).
- For metadata/description replay, verify the complete current requested postcondition—title, metadata, sections, and rendered description—before returning `recovered`; return nonzero partial recovery on drift. Add one-field-at-a-time drift tests that preserve the historical operation marker.
- Make the approved focused-test/package-format sequence reproducible without manual cleanup, for example by invoking Jest without coverage for focused runs or excluding disposable coverage output from package formatting, then rerun the exact verification matrix.

