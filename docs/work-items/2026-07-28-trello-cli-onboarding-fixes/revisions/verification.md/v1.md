Work item: 2026-07-28-trello-cli-onboarding-fixes
Artifact: verification
Revision: 1
Prerequisites: contract@1,plan@2,implementation@1
Status: superseded

# Commands

1. `git status --short` plus programmatic comparison with `implementation-report.md` and the plan's affected paths — changed-path scope and reporting audit.
2. `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --runTestsByPath src/cli.test.ts` — planned `proposed` mutation result and CLI contract.
3. `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --runTestsByPath src/work-unit.test.ts src/config.test.ts src/transition.test.ts src/board.test.ts` — In Design schema, seven-list mapping, board resolution, and Ready gating.
4. `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --runTestsByPath src/read-commands.test.ts` — mixed Inbox listing.
5. `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --runTestsByPath src/design.test.ts` — same-card design lifecycle and mutation recovery.
6. `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --runTestsByPath src/create.test.ts src/cli.test.ts src/design.test.ts src/read-commands.test.ts` — draft-create compatibility alias and command routing.
7. `pnpm --filter @jz/ai-arsenal-trello-work-cli exec jest --runInBand --runTestsByPath src/docs.test.ts src/list-management.test.ts` — documentation assets and seven-list lifecycle.
8. `pnpm --filter @jz/ai-arsenal-trello-work-cli format`, `lint`, `typecheck`, and `test` — package gates.
9. `pnpm format`, `pnpm lint`, `pnpm typecheck`, and `pnpm test` — root gates.
10. `pnpm test:workflow` and `node scripts/validate-living-workflow.mjs` — workflow gates.
11. `git diff --check` — whitespace integrity.
12. `pnpm --filter @jz/ai-arsenal-trello-work-cli validate` — not run because it performs `pnpm pack`, which this verification stage explicitly prohibits.
13. `pnpm add --global ./packages/trello-work-cli` and the TestingBoard onboarding workbook — not run because this verification stage explicitly prohibits global installation and permits no durable external Trello mutation.

## Exit codes

1. Not applicable for the manual/programmatic audit; comparison passed.
2. `0` — 42 tests passed.
3. `0` — 51 tests passed.
4. `0` — 10 tests passed.
5. `0` — 7 tests passed.
6. `0` — 70 tests passed.
7. `0` — 31 tests passed.
8. `0`, `0`, `0`, `0` — package test total: 19 suites passed, 218 tests passed, 1 skipped.
9. `0`, `0`, `0`, `0` — all three root Turbo tasks passed for each gate.
10. `0`, `0` — 30 workflow tests passed and living-workflow validation passed.
11. `0`.
12. Not run; prohibited by stage boundary.
13. Not run; prohibited by stage boundary.

## Observed result

- All implemented offline acceptance seams passed on the current worktree.
- All product/documentation paths in `git status --short` are listed in the implementation report and approved plan; workflow artifacts and `NEXT.md` are governed pipeline state.
- Ordinary Inbox cards, claimed malformed Work Units, the In Design state, same-card design start, replay/recovery handling, Ready gating, draft creation, command routing, seven-list initialization, docs, and the corrected workbook have passing focused coverage.
- The approved plan nevertheless requires two verification actions that the governing verification skill forbids: package validation through a packing action, and global installation plus durable live Trello mutation. Therefore complete plan/contract evidence cannot be observed in this stage.

## Status

Failed.

## Remaining failures

- The approved plan must be revised so repository verification contains only stage-permitted offline evidence.
- Strict package packing validation and the live TestingBoard onboarding run must be treated as a separately authorized post-capture operational validation, not as acceptance evidence inside `verify-monorepo-change`.
