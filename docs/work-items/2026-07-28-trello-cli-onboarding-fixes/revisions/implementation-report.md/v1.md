Work item: 2026-07-28-trello-cli-onboarding-fixes
Artifact: implementation
Revision: 1
Prerequisites: contract@1,plan@2,approval@2
Status: superseded

# Changed paths

- `packages/trello-work-cli/README.md`
- `packages/trello-work-cli/assets/work-guide.md`
- `packages/trello-work-cli/assets/work-unit-metadata.schema.json`
- `packages/trello-work-cli/package.json`
- `packages/trello-work-cli/src/board.test.ts`
- `packages/trello-work-cli/src/cli.test.ts`
- `packages/trello-work-cli/src/cli.ts`
- `packages/trello-work-cli/src/command-catalog.ts`
- `packages/trello-work-cli/src/config.test.ts`
- `packages/trello-work-cli/src/config.ts`
- `packages/trello-work-cli/src/design.test.ts`
- `packages/trello-work-cli/src/design.ts`
- `packages/trello-work-cli/src/docs.test.ts`
- `packages/trello-work-cli/src/list-management.test.ts`
- `packages/trello-work-cli/src/read-commands.test.ts`
- `packages/trello-work-cli/src/read-commands.ts`
- `packages/trello-work-cli/src/transition.test.ts`
- `packages/trello-work-cli/src/transition.ts`
- `packages/trello-work-cli/src/work-unit.test.ts`
- `packages/trello-work-cli/src/work-unit.ts`
- `packages/trello-work-cli/test/fixtures/valid-in-design.md`
- `packages/trello-work-cli/test/live-trello.e2e.test.ts`
- `docs/operations/trello-work-cli-onboarding-workbook.md`

## Decisions

- A planned mutation envelope accepts `proposed` in addition to the existing `plan` and `draft` payloads.
- `In Design` is the seventh canonical list and `in_design` is the partial Work Unit status; canonical sections remain required while explicit pending content and Open Questions are allowed.
- `work inbox list` exposes ordinary Inbox intake cards. Work Unit list commands skip clearly ordinary cards but continue to fail closed on malformed cards that claim Work Unit identity.
- `work design start` promotes the selected Inbox card in place, preserving card identity, and uses the existing operation-record recovery model for replay, collisions, stale versions, ambiguous writes, and read-back failures.
- `work draft create` is the explicit structured-draft entry point. `work create` remains a deprecated compatibility alias.
- In Design to Ready is rejected while explicit `Pending:` content or material Open Questions remain.

## Tests

- RED then GREEN evidence was captured for planned `proposed` results, In Design parsing and mapping, mixed Inbox listing, design start, Ready gating, and CLI routing.
- Focused implementation suites passed, including the design suite (7 tests), Ready-gating suite (8 tests), and aggregate focused set (122 tests).
- `pnpm --filter @jz/ai-arsenal-trello-work-cli format` passed.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli lint` passed.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli typecheck` passed.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli test` passed: 19 suites, 218 passed, 1 skipped.
- `pnpm format` passed after removing generated coverage output from the preceding test run.
- `pnpm lint`, `pnpm typecheck`, and `pnpm test` passed; root tests included 154 Features CLI tests and 218 Trello Work CLI tests.
- `pnpm test:workflow` passed: 30 tests.
- `node scripts/validate-living-workflow.mjs` passed.
- `pnpm --filter @jz/ai-arsenal-trello-work-cli validate` passed with strict publint package validation.
- Generated `.tgz` files were removed and the repository-wide tarball count was verified as zero.

## Deviations

None.
