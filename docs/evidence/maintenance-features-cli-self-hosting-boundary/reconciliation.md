# features-cli Self-Hosting Boundary Reconciliation

Phase: Maintenance — features-cli self-hosting boundary

Verification: The scoped package instructions explicitly separate package maintenance from consumer feature workflows.

Resulting system state: `features-cli` is not an authority for managing development of `packages/features-cli`; the monorepo living-plan workflow remains that authority.

Discoveries: No public CLI behavior, persisted schema, package boundary, or release direction changed.

Canonical plan updates: Current verified state records the scoped self-hosting boundary.

NEXT.md update: No change. Confirming the new `master` CI runs remains the next action.

Approval required: None. The user explicitly requested this repository-operating rule.
