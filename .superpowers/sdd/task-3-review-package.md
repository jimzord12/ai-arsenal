# Task 3 review package

Base: `58bf918` (preflight baseline; the task is intentionally uncommitted)

Head: working tree

## Added paths

- `.agents/skills/capture-monorepo-change/SKILL.md`
- `.agents/skills/capture-monorepo-change/agents/openai.yaml`

Both paths are untracked, so Git has no commit-range diff. Treat their complete
current contents as the added-file diff. Read no other changed path.
